# BF10. Матрица Эйзенхауэра (Backlog Triage)

## Обзор

Визуальная матрица 2×2 для приоритизации orphan-задач (stories, tasks, bugs без эпика). Тимлид распределяет задачи по квадрантам, назначает роль, даёт оценку и создаёт subtasks — подготавливая задачу к работе. Задачи из матрицы интегрируются с автопланером: при простое роли система рекомендует задачи из П1 → П2.

**Проблема:** В Jira накапливаются десятки задач без эпика — техдолг, баги, мелкие улучшения. Сейчас Lead Board их игнорирует (работает только с эпиками и их детьми). Эти задачи не попадают в forecast, не учитываются при планировании, и при простое ролей команда не знает что брать.

**Решение:** Матрица Эйзенхауэра с полноценной подготовкой задач к работе (оценка, роль, subtasks) и автоматическими рекомендациями при простое.

---

## Матрица

```
              СРОЧНО                         НЕ СРОЧНО
       ┌─────────────────────────┬─────────────────────────┐
       │  🟡 П1                   │  🟠 П2                   │
ВАЖНО  │  Важно и срочно         │  Важно и не срочно       │
       │  НАИВЫСШИЙ приоритет    │  ВЫСОКИЙ приоритет       │
       │                         │                          │
       │  Прод-баги, секьюрити,  │  Техдолг, рефакторинг,   │
       │  блокеры, SLA           │  улучшение перформанса   │
       ├─────────────────────────┼──────────────────────────┤
       │  🟣 П3                   │  🟢 П4                   │
НЕ     │  Не важно и срочно      │  Не срочно и не важно    │
ВАЖНО  │  УМЕРЕННЫЙ приоритет    │  НИЗКИЙ приоритет        │
       │                         │                          │
       │  Мелкие фиксы с         │  Nice-to-have, хотелки,  │
       │  дедлайном, косметика   │  "было бы круто"         │
       └─────────────────────────┴──────────────────────────┘
```

### Действия по квадрантам

| Квадрант | Приоритет | Действие | Рекомендация автопланера |
|----------|-----------|----------|--------------------------|
| **П1** | Наивысший | Делать немедленно | Первый в очереди при любом простое |
| **П2** | Высокий | Планировать, заполнять простои | Второй после П1 |
| **П3** | Умеренный | Делегировать, брать при наличии времени | Третий, предпочтительно джунам |
| **П4** | Низкий | Бэклог, брать последним или Won't Fix | Только если нет задач из П1-П3 |

---

## Источник задач

### Какие задачи попадают в матрицу

**Orphan-задачи** — issues из Jira, у которых:
- `parent IS NULL` (нет parent epic)
- `board_category = 'STORY'` (Story, Task, Bug — но НЕ subtask и НЕ epic)
- `status_category != 'DONE'` (не завершённые)
- Принадлежат проекту, привязанному к команде

**Важно:** Subtask не является самостоятельной задачей. Subtask всегда принадлежит родителю и не может существовать отдельно — это ограничение платформы. В матрице оперируем только Story/Task/Bug. Subtasks отображаются внутри родительской задачи (как прогресс и детализация), но не как отдельные элементы матрицы.

### Автоматическое подтягивание

При каждом sync из Jira orphan-задачи автоматически попадают в секцию "Нераспределённые" матрицы. Тимлид периодически проводит triage — распределяет по квадрантам.

---

## Экран матрицы

### Основной layout

```
┌─ Матрица бэклога ─ Team Alpha ──────────────────────────────────────────────┐
│                                                                              │
│  [Фильтр по роли ▼]  [Фильтр по типу ▼]  [Фильтр по labels ▼]  [🔄 Sync] │
│                                                                              │
│  ┌─── Здоровье бэклога ─────────────────────────────────────────────────┐   │
│  │  🟡 П1: 4 (18%) 48ч   🟠 П2: 8 (36%) 96ч                           │   │
│  │  🟣 П3: 5 (23%) 24ч   🟢 П4: 5 (23%) 18ч   Нераспределённые: 12    │   │
│  │  ⚠️ П1 > 15% — много критичных задач                                 │   │
│  │  💡 QA простой 16ч — 3 задачи из П1-П2 подходят                      │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│         СРОЧНО                              НЕ СРОЧНО                        │
│  ┌───────────────────────────┐  ┌───────────────────────────┐                │
│  │ 🟡 П1 Важно+срочно    4  │  │ 🟠 П2 Важно+не срочно  8 │                │
│  │ наивысший         48ч    │  │ высокий          96ч      │   ВАЖНО        │
│  │                          │  │                           │                │
│  │ ┌────────────────────┐   │  │ ┌────────────────────┐    │                │
│  │ │🔴 LB-201 Bug       │   │  │ │🟡 LB-195 Story     │    │                │
│  │ │Утечка памяти sync  │   │  │ │Рефакторинг Client  │    │                │
│  │ │DEV · 16ч · ⏰2д    │   │  │ │DEV · 24ч · 📋3 sub │    │                │
│  │ │👤Иванов · 📋2/3    │   │  │ │👤— · 📋0 sub       │    │                │
│  │ └────────────────────┘   │  │ └────────────────────┘    │                │
│  │ ┌────────────────────┐   │  │ ┌────────────────────┐    │                │
│  │ │🔴 LB-215 Bug       │   │  │ │🟡 LB-180 Task      │    │                │
│  │ │500 error /api/sync │   │  │ │Обновить Spring 3.4 │    │                │
│  │ │DEV · 8ч · ⏰сегодня│   │  │ │DEV · 16ч · 📋0 sub │    │                │
│  │ │👤— · 📋0 sub       │   │  │ │👤— · 📋0 sub       │    │                │
│  │ └────────────────────┘   │  │ └────────────────────┘    │                │
│  │         ...              │  │         ...               │                │
│  └───────────────────────────┘  └───────────────────────────┘                │
│  ┌───────────────────────────┐  ┌───────────────────────────┐                │
│  │ 🟣 П3 Не важно+срочно 5  │  │ 🟢 П4 Не важно+не сроч 5 │                │
│  │ умеренный         24ч    │  │ низкий           18ч      │   НЕ ВАЖНО    │
│  │                          │  │                           │                │
│  │ ┌────────────────────┐   │  │ ┌────────────────────┐    │                │
│  │ │🟡 LB-220 Task      │   │  │ │⚪ LB-150 Story     │    │                │
│  │ │Поправить тексты    │   │  │ │Dark mode           │    │                │
│  │ │DEV · 4ч · ⏰пт     │   │  │ │DEV · 8ч · 📋0 sub  │    │                │
│  │ │👤— · 📋0 sub       │   │  │ │👤— · 📋0 sub       │    │                │
│  │ └────────────────────┘   │  │ └────────────────────┘    │                │
│  │         ...              │  │         ...               │                │
│  └───────────────────────────┘  └───────────────────────────┘                │
│                                                                              │
│  ┌─── Нераспределённые (12) ── drag в квадрант ─────────────────────────┐   │
│  │ LB-225 Bug  "Кнопка не работает"     · ⚠️ без оценки · без роли     │   │
│  │ LB-230 Story "Экспорт в CSV"         · DEV 16ч                       │   │
│  │ LB-231 Task  "Добавить индекс"       · DEV 2ч                        │   │
│  │ ...ещё 9                                                   [Показать] │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────┘
```

### Карточка задачи

```
┌────────────────────────────────┐
│ 🔴 LB-201  Bug          ⋮     │  ← Jira priority (иконка) + key + type
│ Утечка памяти в sync service   │  ← summary
│                                │
│ 🏷 DEV · ⏱ 16ч · ⏰ 2 дня      │  ← роль · estimate · due date/возраст
│ 👤 Иванов А. · 📋 2/3 subtasks │  ← assignee · subtask прогресс
│ 🔗 blocks LB-198               │  ← зависимости (если есть)
└────────────────────────────────┘
```

**Поля на карточке:**

| Поле | Источник | Отображение |
|------|----------|-------------|
| Key | Jira | `LB-201` |
| Issue Type | Jira | Иконка + текст (Bug, Story, Task) |
| Summary | Jira | Заголовок задачи |
| Jira Priority | Jira | Цветная иконка (🔴🟡🟢⚪) |
| Роли (из subtasks) | Jira subtasks | Badges: `SA` `DEV` `QA` (какие роли задействованы) |
| **Estimate** | Тимлид / Jira | Часы: `16ч` |
| Status | Jira | Текст/badge |
| Assignee | Jira | Имя или `—` |
| Due date / Возраст | Jira | `⏰ 2 дня` или `📅 5 фев` |
| Subtasks | Jira | `📋 2/3` (выполнено/всего) |
| Links | Jira | `🔗 blocks LB-198` (если есть) |
| Labels | Jira | Теги (если есть) |

**Индикаторы готовности к работе:**
- ⚠️ Без оценки — задача не готова для автопланера
- ⚠️ Без subtasks — роли не определены, автопланер не сможет рекомендовать
- ✅ Готова — есть оценка и subtasks с ролями

---

## Детальная панель задачи

Открывается при клике на карточку. Содержит полную информацию и инструменты triage.

```
┌─ LB-201: Утечка памяти в sync service ──────────────────────────┐
│                                                                   │
│  ─── Информация из Jira ────────────────────────────────────     │
│                                                                   │
│  Тип: Bug            Статус: Open          Приоритет: Critical   │
│  Создана: 5 фев 2026 Возраст: 5 дней       Due date: 12 фев     │
│  Assignee: Иванов А.                        Reporter: Петров И.  │
│  Labels: production, backend, memory-leak                        │
│  Компонент: sync-service                                         │
│                                                                   │
│  Описание:                                                       │
│  При синхронизации проекта с >1000 задач происходит OOM...       │
│  [Показать полностью]                                            │
│                                                                   │
│  Зависимости:                                                    │
│  🔗 blocks: LB-198 "Увеличить лимит sync до 5000"               │
│                                                                   │
│  ─── Triage ─────────────────────────────────────────────────    │
│                                                                   │
│  Квадрант:  [🟡 П1 ▼]      (авто: П1 — Critical + due date)    │
│  Оценка:    [16] ч          (Jira estimate: 16ч)                │
│                                                                   │
│  ─── Subtasks (2/3) ─────────────────────────────────────────    │
│                                                                   │
│  ✅ LB-201-1  Аналитика: локализовать утечку     SA   4ч  Done  │
│  🔄 LB-201-2  Исправить memory leak              DEV  8ч  In Pr │
│  ⬜ LB-201-3  Тестирование под нагрузкой          QA   4ч  New   │
│                                                                   │
│  ─── или если subtasks нет ──────────────────────────────────    │
│                                                                   │
│  📋 Subtasks: нет                                                 │
│  [+ Создать subtasks по шаблону]                                 │
│                                                                   │
│                                           [Сохранить]  [Закрыть] │
└───────────────────────────────────────────────────────────────────┘
```

---

## Создание subtasks

### Шаблон по ролям pipeline

Когда subtasks нет, тимлид создаёт их прямо из детальной панели. Система берёт pipeline ролей из конфигурации (BF9) и предлагает шаблон:

```
┌─ Создание subtasks для LB-201 ───────────────────────────────────┐
│                                                                    │
│  Pipeline: SA → DEV → QA (из конфигурации проекта)                │
│  Estimate задачи: 16ч                                             │
│                                                                    │
│  ☑ SA   "Анализ: LB-201 Утечка памяти"        [ 2]ч  👤 [авто ▼]│
│  ☑ DEV  "Разработка: LB-201 Утечка памяти"    [10]ч  👤 [авто ▼]│
│  ☑ QA   "Тестирование: LB-201 Утечка памяти"  [ 4]ч  👤 [авто ▼]│
│  ☐ DEVOPS  (не нужен — снять галку)            [ 0]ч             │
│                                                            ───────│
│  Сумма subtasks:  16ч  (= estimate задачи ✅)                     │
│                                                                    │
│  ⚠️ Сумма не совпадает с estimate → показать предупреждение       │
│                                                                    │
│  [Создать в Jira]  [Отмена]                                      │
└────────────────────────────────────────────────────────────────────┘
```

### Логика создания

1. Берём **список ролей в порядке pipeline** из BF9 (`WorkflowConfigService.getRolesInPipelineOrder()`)
2. Для каждой роли предлагаем subtask с шаблонным именем: `"{Роль display_name}: {parentKey} {parentSummary}"`
3. Estimate по умолчанию: распределяем пропорционально (или поровну)
4. Тимлид:
   - Убирает ненужные роли (снимает галку)
   - Корректирует оценки
   - Выбирает assignee (или оставляет "авто" — автопланер назначит)
5. **Валидация:** сумма subtask estimates = estimate родительской задачи (warning если не совпадает)
6. "Создать в Jira" → вызов `TrackerProvider.createSubtask()` для каждого subtask
7. При следующем sync subtasks подтягиваются обратно с Jira key

### Issue type для subtasks

Используется маппинг из BF9:
- Для роли SA → issue type из `issue_type_mappings` где `workflow_role.code = 'SA'` (например "Аналитика")
- Для роли DEV → issue type с `is_default = true` (например "Sub-task")
- Для роли QA → issue type из маппинга (например "Тестирование")

---

## Автоматическое предложение квадранта

При попадании новой задачи в "Нераспределённые" система анализирует сигналы и предлагает квадрант.

### Сигналы срочности

| Сигнал | Вес | Источник |
|--------|-----|----------|
| Jira Priority = Blocker/Critical | +3 | Jira sync |
| Due date ≤ 5 рабочих дней | +3 | Jira sync |
| Due date ≤ 15 рабочих дней | +1 | Jira sync |
| Возраст > 30 дней | +1 | Jira created date |
| Блокирует другие задачи | +1 | Jira links |

**Срочность:** сумма весов ≥ 3 → СРОЧНО, иначе НЕ СРОЧНО

### Сигналы важности

| Сигнал | Вес | Источник |
|--------|-----|----------|
| Jira Priority = Blocker/Critical/Major | +2 | Jira sync |
| Блокирует ≥ 2 задачи | +2 | Jira links |
| Связана с активным эпиком (relates to) | +1 | Jira links |
| Issue type = Bug | +1 | Jira sync |

**Важность:** сумма весов ≥ 3 → ВАЖНО, иначе НЕ ВАЖНО

### Результат

| Важно | Срочно | Квадрант |
|-------|--------|----------|
| ✅ | ✅ | П1 |
| ✅ | ❌ | П2 |
| ❌ | ✅ | П3 |
| ❌ | ❌ | П4 |

Предложение показывается как hint: `(авто: П1 — Critical + due date)`. Тимлид подтверждает или меняет.

---

## Статистика и пороги

### Панель статистики

```
┌─── Здоровье бэклога ─────────────────────────────────────────┐
│                                                               │
│  Распределение:                                              │
│  🟡 П1: 4 задачи (18%)  ██░░░░░░░░  48ч                     │
│  🟠 П2: 8 задач  (36%)  ████░░░░░░  96ч                     │
│  🟣 П3: 5 задач  (23%)  ██░░░░░░░░  24ч                     │
│  🟢 П4: 5 задач  (23%)  ██░░░░░░░░  18ч                     │
│                                                               │
│  Алерты:                                                     │
│  ⚠️ П1 = 18% (порог: 15%) — много критичных задач            │
│  ✅ П2 = 36% (норма: 30-50%)                                  │
│  ✅ П3 = 23% (норма: < 30%)                                   │
│  ✅ П4 = 23% (норма: < 30%)                                   │
│                                                               │
│  Готовность к работе:                                        │
│  ✅ С оценкой: 19/22 (86%)                                    │
│  ⚠️ Без subtasks: 8/22 (36%)                                  │
│  ⚠️ Без subtasks: 8/22 — роли не определены                     │
│                                                               │
│  По ролям:                                                    │
│  DEV: 14 задач · 120ч    SA: 4 задачи · 32ч                  │
│  QA:  4 задачи · 34ч                                         │
│                                                               │
│  💡 Рекомендация автопланера:                                 │
│  QA простой 16ч — 3 задачи из П1-П2 подходят для QA         │
│  (LB-201-3, LB-210, LB-218)                    [Подробнее →] │
└───────────────────────────────────────────────────────────────┘
```

### Настраиваемые пороги

| Метрика | Порог по умолчанию | Уровень | Описание |
|---------|-------------------|---------|----------|
| П1 % от общего | > 15% | ⚠️ WARNING | Слишком много критичных — разобрать или переоценить |
| П1 задача старше | > 5 рабочих дней | 🔴 CRITICAL | Критичная задача слишком долго не берётся |
| П4 % от общего | > 40% | ⚠️ WARNING | Много мусора — почистить (Won't Fix) |
| Без оценки % | > 20% | ⚠️ WARNING | Задачи не готовы для автопланера |
| Без subtasks % | > 30% | ⚠️ WARNING | Роли не определены, автопланер не сможет рекомендовать |
| Нераспределённые | > 20 задач | ⚠️ WARNING | Пора провести triage |
| Общий объём П1 | > capacity команды на неделю | 🔴 CRITICAL | Невозможно закрыть все П1 за неделю |

### Автомиграция между квадрантами

Система автоматически пересматривает срочность:

| Триггер | Действие |
|---------|----------|
| Due date задачи ≤ 5 дней, задача в П2 | Предложить перенос в П1: `"LB-180 due date через 3 дня — перенести в П1?"` |
| Due date задачи ≤ 5 дней, задача в П4 | Предложить перенос в П3 |
| Задача в П1 > 10 дней без движения | Alert: `"LB-201 в П1 уже 10 дней без прогресса"` |
| Задача в П4 > 60 дней | Предложить Won't Fix: `"LB-150 в П4 уже 2 месяца — закрыть?"` |

Автомиграция не перемещает задачи автоматически — только предлагает. Тимлид подтверждает.

---

## Интеграция с автопланером

### Как работает

`RoleLoadService` уже определяет простой ролей (например: "QA свободен 16ч на этой неделе"). BF10 добавляет рекомендации из матрицы:

```
Простой роли обнаружен
        │
        ▼
Фильтр задач из матрицы:
  1. Роль совпадает (или задача без роли → default role)
  2. Есть estimate
  3. Estimate ≤ доступное время простоя
        │
        ▼
Сортировка:
  1. П1 → П2 → П3 → П4 (по квадранту)
  2. Внутри квадранта: по Jira priority → по возрасту
        │
        ▼
Рекомендация тимлиду:
  "QA свободен 16ч. Рекомендуем:
   1. LB-201 Утечка памяти sync (П1, QA-subtask 4ч)
   2. LB-210 Регрессия API (П2, 8ч)
   3. LB-218 Smoke test UI (П2, 4ч)"
```

### Условия попадания задачи в рекомендации

Задача (Story/Task/Bug) готова для автопланера если:
- ✅ Назначен квадрант (П1-П4)
- ✅ Есть estimate
- ✅ Есть subtasks с ролями (чтобы автопланер знал какая роль нужна)
- ✅ Статус != DONE

Рекомендация привязана к родительской задаче, а не к subtask. Автопланер рекомендует задачу целиком, указывая какой subtask (роль) актуален для простаивающей роли.

### Отображение рекомендаций

Рекомендации показываются в:
1. **Панель статистики матрицы** — внизу блока
2. **RoleLoadService alerts** — существующий механизм алертов по ролям
3. **BF7 Notifications** (будущее) — уведомление тимлиду

### API рекомендаций

```
GET /api/matrix/recommendations?teamId=1
→ {
    "recommendations": [
      {
        "role": "QA",
        "idleHours": 16,
        "tasks": [
          {
            "issueKey": "LB-201",
            "quadrant": "P1",
            "summary": "Утечка памяти sync",
            "subtaskForRole": { "key": "LB-201-3", "summary": "Тестирование", "estimate": 4 }
          },
          {
            "issueKey": "LB-210",
            "quadrant": "P2",
            "summary": "Регрессия API",
            "subtaskForRole": { "key": "LB-210-2", "summary": "Тестирование", "estimate": 8 }
          }
        ]
      }
    ]
  }
```

---

## DB Schema

```sql
-- Миграция: V__create_eisenhower_matrix.sql

-- ============================================
-- 1. Triage-данные задачи (расширение jira_issues)
-- ============================================

ALTER TABLE jira_issues ADD COLUMN eisenhower_quadrant VARCHAR(10);
  -- P1, P2, P3, P4, NULL (нераспределённая)

ALTER TABLE jira_issues ADD COLUMN eisenhower_auto_quadrant VARCHAR(10);
  -- Автоматическое предложение (для показа hint)

ALTER TABLE jira_issues ADD COLUMN triaged_at TIMESTAMP;
  -- Когда была размещена в квадрант

ALTER TABLE jira_issues ADD COLUMN triaged_by BIGINT;
  -- Кто разместил (user_id)

CREATE INDEX idx_jira_issues_quadrant ON jira_issues(eisenhower_quadrant)
  WHERE eisenhower_quadrant IS NOT NULL;

CREATE INDEX idx_jira_issues_orphan ON jira_issues(parent_key, board_category, eisenhower_quadrant)
  WHERE parent_key IS NULL;

-- ============================================
-- 2. Пороги матрицы (per-team настройки)
-- ============================================

CREATE TABLE matrix_thresholds (
    id              BIGSERIAL PRIMARY KEY,
    team_id         BIGINT NOT NULL REFERENCES teams(id) ON DELETE CASCADE,
    threshold_key   VARCHAR(100) NOT NULL,   -- P1_PERCENT, P4_PERCENT, NO_ESTIMATE_PERCENT, ...
    threshold_value NUMERIC NOT NULL,        -- 15, 40, 20, ...
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW(),
    UNIQUE(team_id, threshold_key)
);

-- ============================================
-- 3. История triage (аудит)
-- ============================================

CREATE TABLE triage_history (
    id              BIGSERIAL PRIMARY KEY,
    issue_id        BIGINT NOT NULL REFERENCES jira_issues(id),
    user_id         BIGINT,
    action          VARCHAR(50) NOT NULL,    -- QUADRANT_SET, QUADRANT_CHANGED, ESTIMATE_SET, SUBTASKS_CREATED
    old_value       VARCHAR(200),
    new_value       VARCHAR(200),
    created_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_triage_history_issue ON triage_history(issue_id, created_at DESC);
```

---

## Архитектура

### Backend

```
com.leadboard.matrix/
├── entity/
│   └── MatrixThresholdEntity          — Пороги per-team
├── repository/
│   └── MatrixThresholdRepository
├── service/
│   ├── MatrixService                  — Основной сервис: получение задач, статистика, triage
│   ├── MatrixAutoSuggestService       — Автоопределение квадранта и роли
│   ├── MatrixRecommendationService    — Рекомендации при простое ролей
│   ├── MatrixStatisticsService        — Расчёт статистики и алертов по порогам
│   ├── MatrixSubtaskService           — Создание subtasks из шаблона
│   └── MatrixAutoMigrationService     — Автопредложения миграции между квадрантами
├── controller/
│   └── MatrixController               — REST API
├── dto/
│   ├── MatrixViewDto                  — Полное представление матрицы (4 квадранта + нераспределённые)
│   ├── MatrixCardDto                  — Карточка задачи
│   ├── MatrixTriageDto                — Данные triage (квадрант, estimate)
│   ├── MatrixStatisticsDto            — Статистика и алерты
│   ├── MatrixRecommendationDto        — Рекомендации при простое
│   ├── SubtaskTemplateDto             — Шаблон создания subtasks
│   └── MatrixThresholdDto             — Настройки порогов
└── scheduler/
    └── MatrixAutoMigrationScheduler   — Периодическая проверка автомиграции
```

### Frontend

```
frontend/src/
├── pages/
│   └── MatrixPage.tsx                 — Основная страница матрицы
├── components/
│   └── matrix/
│       ├── MatrixGrid.tsx             — Сетка 2×2 с квадрантами
│       ├── MatrixQuadrant.tsx         — Один квадрант (drop zone)
│       ├── MatrixCard.tsx             — Карточка задачи (draggable)
│       ├── MatrixDetailPanel.tsx      — Детальная панель (slide-over)
│       ├── MatrixTriageForm.tsx       — Форма triage (квадрант, роль, estimate)
│       ├── MatrixSubtaskCreator.tsx   — Создание subtasks
│       ├── MatrixStatistics.tsx       — Панель статистики
│       ├── MatrixRecommendations.tsx  — Рекомендации автопланера
│       ├── MatrixUnassigned.tsx       — Секция нераспределённых задач
│       ├── MatrixThresholdSettings.tsx— Настройка порогов
│       └── MatrixAutoMigrationAlert.tsx — Алерты автомиграции
├── api/
│   └── matrixApi.ts                   — API клиент
└── hooks/
    ├── useMatrixDragDrop.ts           — Drag & drop логика
    └── useMatrixStatistics.ts         — Статистика с polling
```

### Роутинг

```
/matrix            — Основной экран матрицы
/matrix/settings   — Настройка порогов (Admin/TeamLead)
```

---

## API Endpoints

### Матрица

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/matrix?teamId={id}` | Полная матрица: 4 квадранта + нераспределённые + статистика | TeamLead+ |
| GET | `/api/matrix/unassigned?teamId={id}` | Только нераспределённые задачи | TeamLead+ |
| GET | `/api/matrix/statistics?teamId={id}` | Статистика и алерты | TeamLead+ |

### Triage

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| PUT | `/api/matrix/triage` | Назначить квадрант и estimate `{issueId, quadrant, estimate?}` | TeamLead+ |
| PUT | `/api/matrix/triage/batch` | Batch triage нескольких задач | TeamLead+ |
| GET | `/api/matrix/auto-suggest/{issueId}` | Автоматическое предложение квадранта и роли | TeamLead+ |

### Subtasks

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/matrix/subtask-template/{issueId}` | Шаблон subtasks по pipeline | TeamLead+ |
| POST | `/api/matrix/subtasks` | Создать subtasks в Jira `{issueId, subtasks: [{role, summary, estimate, assignee?}]}` | TeamLead+ |

### Рекомендации

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/matrix/recommendations?teamId={id}` | Рекомендации при простое ролей | TeamLead+ |

### Настройки

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/matrix/thresholds?teamId={id}` | Текущие пороги | TeamLead+ |
| PUT | `/api/matrix/thresholds` | Обновить пороги `{teamId, thresholds: [{key, value}]}` | Admin |

### История

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/matrix/history/{issueId}` | История triage задачи | TeamLead+ |

---

## Безопасность

- **Просмотр матрицы:** TeamLead+ (своя команда), Admin (все)
- **Triage (назначение квадранта, роли):** TeamLead+ (своя команда)
- **Создание subtasks в Jira:** TeamLead+ (используется OAuth-токен текущего пользователя)
- **Настройка порогов:** Admin
- **Audit log:** все действия triage записываются в `triage_history`

---

## Зависимости

| Зависимость | Тип | Описание |
|-------------|-----|----------|
| BF9 Workflow Configuration | Требуется | Pipeline ролей для создания subtasks, определение issue types |
| F27 RBAC | Требуется | Доступ TeamLead/Admin |
| RoleLoadService | Используется | Данные о простое ролей для рекомендаций |
| TrackerProvider (BF9) | Используется | Создание subtasks через API трекера |

---

## План реализации (поэтапно)

### Этап 1: DB + MatrixService + API

1. Flyway-миграция: `eisenhower_quadrant`, `eisenhower_auto_quadrant`, `triaged_at`, `triaged_by` в `jira_issues`
2. Flyway-миграция: `matrix_thresholds`, `triage_history`
3. `MatrixService` — получение orphan-задач, группировка по квадрантам
4. `MatrixController` — API: получение матрицы, triage
5. Тесты

### Этап 2: Автопредложение + Статистика

1. `MatrixAutoSuggestService` — анализ сигналов Jira → предложение квадранта и роли
2. `MatrixStatisticsService` — расчёт процентов, алерты по порогам
3. `MatrixAutoMigrationService` — проверка due dates, предложения миграции
4. API endpoints: auto-suggest, statistics
5. Тесты

### Этап 3: Создание subtasks

1. `MatrixSubtaskService` — шаблон subtasks по pipeline из BF9
2. Интеграция с `TrackerProvider.createSubtask()`
3. Валидация: сумма estimates = parent estimate
4. API endpoints: subtask-template, create subtasks
5. Тесты

### Этап 4: Рекомендации автопланера

1. `MatrixRecommendationService` — фильтрация задач по роли, квадранту, estimate
2. Интеграция с `RoleLoadService` — данные о простое
3. API endpoint: recommendations
4. Тесты

### Этап 5: Frontend — Матрица

1. `MatrixPage` — основной layout с сеткой 2×2
2. `MatrixQuadrant` — drop zone
3. `MatrixCard` — draggable карточка с полями
4. `MatrixUnassigned` — нераспределённые задачи
5. Drag & drop: из нераспределённых в квадрант, между квадрантами
6. `matrixApi.ts` — API клиент

### Этап 6: Frontend — Детальная панель и subtasks

1. `MatrixDetailPanel` — slide-over с полной информацией
2. `MatrixTriageForm` — квадрант, роль, estimate с автопредложениями
3. `MatrixSubtaskCreator` — создание subtasks по шаблону
4. Валидация estimate

### Этап 7: Frontend — Статистика и рекомендации

1. `MatrixStatistics` — панель с процентами, алертами, порогами
2. `MatrixRecommendations` — рекомендации при простое
3. `MatrixAutoMigrationAlert` — предложения миграции
4. `MatrixThresholdSettings` — настройка порогов
5. E2E проверка

---

## Будущие расширения

| Расширение | Когда |
|------------|-------|
| Batch triage — выделить несколько задач и назначить квадрант разом | После MVP |
| "Усыновление" — прикрепить orphan-задачу к эпику из матрицы | После MVP |
| Интеграция с BF7 Notifications — уведомления о новых orphan-задачах и автомиграции | После BF7 |
| Экспорт матрицы (PDF/PNG) для стендапов | После MVP |
| Triage session — режим как Planning Poker, задачи по одной | Идея |
| Метрики: среднее время жизни задачи в П1, throughput orphan-задач | После стабилизации |
| AI-подсказки: LLM анализирует описание задачи и предлагает квадрант | После BF6 AI Digest |
