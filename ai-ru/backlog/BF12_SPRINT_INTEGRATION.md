# BF12. Sprint Integration

## Обзор

Поддержка Scrum-спринтов для команд, работающих итерациями. Режим работы (Kanban/Scrum) конфигурируется на уровне команды. Для Scrum-команд: синхронизация спринтов из Jira, визуализация на Timeline, фильтрация на Board, sprint-метрики, Sprint Digest (итоги спринта) и учёт границ спринта в автопланере.

**Проблема:** Lead Board работает как Kanban-доска с непрерывным потоком. Команды на Scrum не видят границ спринтов, не могут оценить capacity спринта, не получают итогов по спринту. Timeline показывает непрерывную ленту без привязки к итерациям.

**Решение:** Настраиваемый режим работы команды. Scrum-команды получают полноценную sprint-интеграцию: синхронизация, визуализация, метрики, дайджест, планирование.

---

## Режим работы команды

### Конфигурация

В настройках команды (TeamMembersPage → Настройки планирования) добавляется поле:

```
methodology: KANBAN | SCRUM
```

- **KANBAN** (default) — текущее поведение, ничего не меняется
- **SCRUM** — активирует sprint-фичи: фильтр на Board, зоны на Timeline, sprint-метрики, Sprint Digest

### Дополнительные поля при SCRUM

| Поле | Тип | Описание |
|------|-----|----------|
| `jiraBoardId` | Long | ID Scrum-доски в Jira (для API спринтов) |
| `sprintCapacitySource` | Enum | `HOURS` (из time tracking) — единица для capacity |

**Определение `jiraBoardId`:**
- При переключении на SCRUM — dropdown со списком досок из Jira Agile API
- `GET /rest/agile/1.0/board?projectKeyOrId={projectKey}&type=scrum`
- Сохраняется в `team_settings` или в расширении `teams` таблицы

---

## Синхронизация спринтов

### Источник данных — Jira Agile REST API

```
GET /rest/agile/1.0/board/{boardId}/sprint?state=active,closed,future
```

**Ответ Jira:**
```json
{
  "values": [
    {
      "id": 123,
      "name": "Sprint 24",
      "state": "closed",      // active | closed | future
      "startDate": "2026-02-03T09:00:00.000Z",
      "endDate": "2026-02-14T18:00:00.000Z",
      "completeDate": "2026-02-14T17:30:00.000Z",
      "goal": "Завершить интеграцию с платёжной системой"
    }
  ]
}
```

**Задачи в спринте:**
```
GET /rest/agile/1.0/sprint/{sprintId}/issue?fields=key,status,timetracking
```

### Что синхронизируем

| Поле Jira | Поле в БД | Описание |
|-----------|-----------|----------|
| `id` | `jira_sprint_id` | ID спринта в Jira |
| `name` | `name` | Название ("Sprint 24") |
| `state` | `state` | active / closed / future |
| `startDate` | `start_date` | Начало спринта |
| `endDate` | `end_date` | Запланированный конец |
| `completeDate` | `complete_date` | Фактический конец (для closed) |
| `goal` | `goal` | Sprint Goal (текст) |

**Маппинг задач:**
- Поле `sprint` на issue в Jira → `sprint_id` на `jira_issues`
- Задача может быть в одном активном/будущем спринте
- Для closed спринтов — фиксируем snapshot (какие задачи были в спринте)

### Периодичность

Синхронизация спринтов вместе с основным sync. При каждом sync:
1. Получить список спринтов для board
2. Обновить/создать записи в `sprints` таблице
3. Обновить `sprint_id` на задачах
4. Для closed спринтов — пересчитать метрики (если ещё не посчитаны)

---

## Схема БД

### Таблица `sprints`

```sql
CREATE TABLE sprints (
    id                BIGSERIAL PRIMARY KEY,
    team_id           BIGINT NOT NULL REFERENCES teams(id),
    jira_sprint_id    BIGINT NOT NULL,
    jira_board_id     BIGINT NOT NULL,
    name              VARCHAR(255) NOT NULL,
    state             VARCHAR(20) NOT NULL,        -- active, closed, future
    start_date        TIMESTAMP,
    end_date          TIMESTAMP,
    complete_date     TIMESTAMP,
    goal              TEXT,
    created_at        TIMESTAMP DEFAULT NOW(),
    updated_at        TIMESTAMP DEFAULT NOW(),

    UNIQUE(team_id, jira_sprint_id)
);

CREATE INDEX idx_sprints_team_state ON sprints(team_id, state);
```

### Таблица `sprint_snapshots` (итоги закрытого спринта)

```sql
CREATE TABLE sprint_snapshots (
    id                BIGSERIAL PRIMARY KEY,
    sprint_id         BIGINT NOT NULL REFERENCES sprints(id),

    -- Scope
    total_issues      INT NOT NULL,
    completed_issues  INT NOT NULL,
    carried_over      INT NOT NULL,           -- не завершённые, перешли в следующий
    removed_issues    INT NOT NULL,           -- убранные из спринта до конца
    added_mid_sprint  INT NOT NULL,           -- добавленные после старта

    -- Hours
    planned_hours     DECIMAL(10,2),          -- сумма оценок на момент старта
    completed_hours   DECIMAL(10,2),          -- сумма time_spent закрытых
    total_logged_hours DECIMAL(10,2),         -- весь залогированный за спринт

    -- Capacity
    capacity_hours    DECIMAL(10,2),          -- team capacity за период спринта
    utilization_pct   DECIMAL(5,2),           -- logged / capacity × 100

    -- Velocity
    velocity_hours    DECIMAL(10,2),          -- часы закрытых задач (оценка)

    -- Goal
    goal_achieved     BOOLEAN,                -- тимлид отмечает вручную

    -- AI Digest
    digest_text       TEXT,                   -- AI-сгенерированный итог

    created_at        TIMESTAMP DEFAULT NOW()
);
```

### Расширение `jira_issues`

```sql
ALTER TABLE jira_issues ADD COLUMN sprint_id BIGINT REFERENCES sprints(id);
CREATE INDEX idx_jira_issues_sprint ON jira_issues(sprint_id);
```

### Расширение `teams`

```sql
ALTER TABLE teams ADD COLUMN methodology VARCHAR(10) DEFAULT 'KANBAN';
ALTER TABLE teams ADD COLUMN jira_board_id BIGINT;
```

---

## API

### Sprint CRUD

```
GET  /api/teams/{teamId}/sprints                    — список спринтов (с фильтром по state)
GET  /api/teams/{teamId}/sprints/active              — текущий активный спринт
GET  /api/teams/{teamId}/sprints/{sprintId}          — детали спринта
GET  /api/teams/{teamId}/sprints/{sprintId}/issues   — задачи в спринте
```

### Sprint Metrics

```
GET  /api/teams/{teamId}/sprints/{sprintId}/metrics  — метрики спринта
GET  /api/teams/{teamId}/sprints/{sprintId}/burndown — burndown chart data
GET  /api/teams/{teamId}/sprints/{sprintId}/digest   — Sprint Digest (AI)
POST /api/teams/{teamId}/sprints/{sprintId}/goal-achieved  — отметить goal achieved
```

### Sprint Velocity History

```
GET  /api/teams/{teamId}/sprint-velocity?last=10     — velocity за последние N спринтов
```

### Ответ — Sprint Details

```json
{
  "id": 1,
  "jiraSprintId": 123,
  "name": "Sprint 24",
  "state": "active",
  "startDate": "2026-02-03",
  "endDate": "2026-02-14",
  "goal": "Завершить интеграцию с платёжной системой",
  "daysRemaining": 3,
  "workDaysRemaining": 3,
  "issues": {
    "total": 18,
    "done": 12,
    "inProgress": 4,
    "todo": 2,
    "completionPct": 67
  },
  "hours": {
    "estimated": 96,
    "logged": 72,
    "remaining": 32,
    "capacity": 120,
    "overflowHours": 0
  }
}
```

### Ответ — Sprint Metrics (closed sprint)

```json
{
  "sprintName": "Sprint 23",
  "startDate": "2026-01-20",
  "endDate": "2026-01-31",
  "completeDate": "2026-01-31",

  "scope": {
    "totalIssues": 20,
    "completed": 16,
    "carriedOver": 3,
    "removed": 1,
    "addedMidSprint": 2,
    "completionRate": 80.0,
    "scopeChangeRate": 15.0
  },

  "hours": {
    "plannedHours": 120,
    "completedHours": 98,
    "totalLoggedHours": 110,
    "capacityHours": 144,
    "utilizationPct": 76.4
  },

  "velocity": {
    "currentSprint": 98,
    "previousSprint": 88,
    "avgLast3Sprints": 92,
    "trend": "UP"
  },

  "goal": {
    "text": "Завершить интеграцию с платёжной системой",
    "achieved": true
  },

  "carryOver": [
    { "key": "PROJ-155", "summary": "Rate limiting API", "estimateH": 12 },
    { "key": "PROJ-162", "summary": "Error monitoring", "estimateH": 8 },
    { "key": "PROJ-168", "summary": "DB index optimization", "estimateH": 4 }
  ]
}
```

---

## UI

### 1. Настройки команды — режим работы

В секции "Настройки планирования" (TeamMembersPage):

```
┌─ Методология ──────────────────────────────────────┐
│                                                      │
│  [○ Kanban]  [● Scrum]                              │
│                                                      │
│  Jira Board:  [Platform Team Board ▼]                │
│                                                      │
└──────────────────────────────────────────────────────┘
```

При выборе Scrum — показать dropdown с Jira-досками типа scrum.

### 2. Board — Sprint Context

При `methodology = SCRUM` сверху доски появляется Sprint Bar:

```
┌─────────────────────────────────────────────────────────────────┐
│  🏃 Sprint 24  │  3 фев — 14 фев  │  Осталось 3 дня           │
│                                                                  │
│  Goal: Завершить интеграцию с платёжной системой                │
│                                                                  │
│  ████████████████████░░░░░░  67%  (12/18 задач)                 │
│  Часы: 72h logged / 96h estimated   Capacity: 120h             │
│                                                                  │
│  [← Sprint 23]  [Sprint 24 ●]  [Sprint 25 →]                   │
└─────────────────────────────────────────────────────────────────┘
```

**Фильтрация:** При выборе спринта Board показывает только задачи этого спринта. "Все" — текущее поведение без фильтра.

### 3. Timeline — Sprint Zones

Вертикальные зоны на Gantt-диаграмме:

```
        │ Sprint 23      │ Sprint 24 (active)  │ Sprint 25      │
        │ 20 янв–31 янв  │ 3 фев–14 фев       │ 17 фев–28 фев │
────────┼────────────────┼────────────────────┼────────────────┤
Epic A  │ ████████████   │ ██████             │                │
  SA    │ ████           │                    │                │
  DEV   │     ██████████ │ ████               │                │
  QA    │                │   ████             │                │
────────┼────────────────┼────────────────────┼────────────────┤
Epic B  │                │ ████████████████   │ ████████       │
  SA    │                │ ████               │                │
  DEV   │                │      █████████████ │ ████           │
  QA    │                │                    │   ████████     │
────────┼────────────────┼────────────────────┼────────────────┤
```

- Текущий спринт — фон с лёгкой заливкой (#deebff)
- Прошлые спринты — серый фон (#f4f5f7)
- Будущие — без заливки, только граница
- Лейблы спринтов в header timeline

### 4. Sprint Metrics Page

Новая вкладка/секция в Metrics при `methodology = SCRUM`:

```
┌─ Sprint 23 — Итоги ─────────────────────────────────────────────┐
│                                                                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │ Закрыто  │  │ Carry-   │  │ Velocity │  │ Capacity │         │
│  │  16/20   │  │ over     │  │   98h    │  │  76%     │         │
│  │   80%    │  │  3 (15%) │  │  ↑ +11%  │  │          │         │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │
│                                                                   │
│  🎯 Goal: Завершить интеграцию с платёжной системой  [✅ Done]  │
│                                                                   │
│  ┌─ Sprint Burndown ───────────────────────────────────────┐     │
│  │                                                          │     │
│  │  hours                                                   │     │
│  │  96 ─╲─ ─ ─ ─ ─ ─ ─ ─ ─ ideal                         │     │
│  │       ╲  ╲                                               │     │
│  │        ╲   ─── actual                                    │     │
│  │         ╲  ╱╲                                            │     │
│  │          ╲╱   ╲                                          │     │
│  │               ╲                                          │     │
│  │   0 ───────────╲─────                                    │     │
│  │     3фев  5  7  9  11  13  14фев                        │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─ Velocity Trend (последние 6 спринтов) ─────────────────┐     │
│  │                                                          │     │
│  │  120h ┊                              ┌───┐               │     │
│  │       ┊              ┌───┐   ┌───┐   │   │   ┌───┐      │     │
│  │   80h ┊      ┌───┐   │   │   │   │   │   │   │   │      │     │
│  │       ┊      │   │   │   │   │   │   │   │   │   │      │     │
│  │       ┊  S19   S20   S21   S22   S23   S24              │     │
│  │       ┊  ─────── avg 92h ────────                        │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─ Scope Change ──────────────────────────────────────────┐     │
│  │  Начальный scope: 18 задач (96h)                        │     │
│  │  + Добавлено в середине спринта: 2 задачи (12h)         │     │
│  │  − Убрано из спринта: 1 задача (4h)                     │     │
│  │  = Финальный scope: 19 задач (104h)                     │     │
│  │  Scope change rate: 15%                                  │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                   │
│  ┌─ Carry-over ────────────────────────────────────────────┐     │
│  │  PROJ-155  Rate limiting API              12h  In Prog  │     │
│  │  PROJ-162  Error monitoring                8h  To Do    │     │
│  │  PROJ-168  DB index optimization           4h  To Do    │     │
│  │                                   Итого:  24h           │     │
│  └──────────────────────────────────────────────────────────┘     │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 5. Sprint Digest (AI)

При закрытии спринта — автоматически генерируется AI-дайджест:

```
┌─ Sprint 24 Digest ──────────────────────────────────────────────┐
│                                                                   │
│  📊 Sprint 24 завершён 14 февраля                                │
│                                                                   │
│  Закрыто 16 из 20 задач (80%). Velocity 98h — на 11% выше       │
│  среднего (92h). Goal "Интеграция с платёжной системой"          │
│  достигнут.                                                      │
│                                                                   │
│  Что сделано:                                                     │
│  • Реализован webhook обработки платежей (PROJ-108, 22h)         │
│  • Кэширование API ответов, ускорение на 40% (PROJ-142, 14h)    │
│  • Миграция на Spring Boot 3.2 (PROJ-125, 20h)                  │
│  • 4 бага исправлены, 2 рефакторинга                             │
│                                                                   │
│  Перенесено в Sprint 25:                                          │
│  • PROJ-155 Rate limiting (12h, In Progress — 60% готово)        │
│  • PROJ-162 Error monitoring (8h, To Do)                         │
│                                                                   │
│  Наблюдения:                                                      │
│  • Алексей Петров — лучший velocity (DSR 0.88)                   │
│  • 2 задачи добавлены после старта спринта (+12h к scope)        │
│  • Carry-over rate 15% — в пределах нормы (порог 20%)            │
│                                                                   │
│  [📋 Копировать]  [📤 Отправить в Slack]                         │
└───────────────────────────────────────────────────────────────────┘
```

**Генерация:** Тот же подход что в BF6 (AI Digest) — собираем данные, формируем prompt, отправляем в LLM. Sprint Digest = AI Digest с контекстом одного спринта.

---

## Автопланер — учёт спринтов

### Текущее поведение (Kanban)

Автопланер раскладывает задачи по дням непрерывно: нашёл свободный слот — поставил задачу.

### Новое поведение (Scrum)

При `methodology = SCRUM` автопланер учитывает границы спринтов:

**Правило 1: Задача не может начаться и закончиться в разных спринтах (рекомендательно)**
- Если задача не влезает в текущий спринт — планер показывает warning
- Не блокирует: тимлид решает сам, разбить задачу или перенести

**Правило 2: Capacity спринта**
- Планер считает capacity = сумма effective hours всех членов × рабочие дни спринта
- Показывает fill rate: "Sprint 25: 80h запланировано / 120h capacity (67%)"
- Warning при > 85%: "Sprint переполнен"

**Правило 3: Sprint overflow**
- Задачи, не влезающие в спринт → автоматически сдвигаются в следующий
- UI показывает "Overflow: 3 задачи (24h) → Sprint 26"

### Sprint Capacity View

```
Sprint 24 (active)              Sprint 25                  Sprint 26
3 фев — 14 фев                 17 фев — 28 фев           3 мар — 14 мар
Capacity: 120h                  Capacity: 120h             Capacity: 120h

████████████████░░░░  80h/120h  ████████████░░░░░░  72h    ███░░░░░░░░░░  24h
       67%                          60%                         20%

Epic A (SA+DEV+QA) 40h         Epic B (DEV+QA) 32h        Overflow: 24h
Epic B (SA) 16h                Epic C (SA+DEV) 40h
Bug fixes 24h
```

---

## Sprint Burndown

### Данные

Для активного спринта — ежедневный snapshot:

```sql
CREATE TABLE sprint_daily_snapshots (
    id                BIGSERIAL PRIMARY KEY,
    sprint_id         BIGINT NOT NULL REFERENCES sprints(id),
    snapshot_date     DATE NOT NULL,
    remaining_hours   DECIMAL(10,2),         -- sum(remaining_estimate) задач в спринте
    completed_hours   DECIMAL(10,2),         -- sum(time_spent) закрытых задач
    total_scope_hours DECIMAL(10,2),         -- sum(original_estimate) всех задач
    issues_remaining  INT,
    issues_completed  INT,

    UNIQUE(sprint_id, snapshot_date)
);
```

Snapshot создаётся при каждом sync (или ежедневно cron). Для closed спринтов — данные зафиксированы.

### Ideal Line

```
idealRemaining(day) = totalScope × (1 - daysPassed / totalWorkDays)
```

### Визуализация

SVG-график (как Epic Burndown в F24):
- X: дни спринта (только рабочие)
- Y: оставшиеся часы
- Ideal line: прямая от totalScope до 0
- Actual line: по daily snapshots

---

## Velocity

### Определение

**Sprint Velocity** = сумма original_estimate закрытых задач за спринт (в часах).

Почему original_estimate, а не time_spent:
- Velocity показывает "сколько работы команда может взять в спринт"
- Если задача оценена в 8h и сделана за 6h — velocity = 8h (команда "потянула" 8h работы)
- time_spent используется для utilization, а не velocity

### Velocity Trend

```json
{
  "sprints": [
    { "name": "Sprint 19", "velocity": 72 },
    { "name": "Sprint 20", "velocity": 80 },
    { "name": "Sprint 21", "velocity": 88 },
    { "name": "Sprint 22", "velocity": 92 },
    { "name": "Sprint 23", "velocity": 88 },
    { "name": "Sprint 24", "velocity": 98 }
  ],
  "avgLast3": 92.7,
  "avgLast5": 89.2,
  "trend": "UP"
}
```

Avg velocity используется для прогнозирования: "при текущей velocity 92h/sprint осталось 3 спринта до завершения эпика".

---

## Carry-over

### Определение

**Carry-over** — задачи, которые были в спринте на момент закрытия, но не были завершены.

### Расчёт

При закрытии спринта (state = closed):
1. Все задачи со `status_category != DONE` → carry-over
2. Фиксируем в `sprint_snapshots.carried_over`
3. Эти задачи автоматически попадают в следующий спринт (Jira делает это сама)

### Carry-over Rate

```
carryOverRate = carriedOver / totalIssues × 100%
```

**Пороги:**
- < 10% — отлично (зелёный)
- 10-20% — нормально (жёлтый)
- > 20% — проблема (красный): команда берёт слишком много или плохо оценивает

---

## Scope Change

### Определение

Задачи добавленные в спринт после его старта или убранные до завершения.

### Отслеживание

При sync сравниваем текущий список задач спринта с предыдущим snapshot:
- Новые задачи → `added_mid_sprint++`
- Пропавшие (не закрытые) задачи → `removed_issues++`

### Scope Change Rate

```
scopeChangeRate = (addedMidSprint + removedIssues) / initialScope × 100%
```

**Пороги:**
- < 10% — стабильный scope
- 10-20% — допустимо
- > 20% — частые изменения, сигнал проблемы с планированием

---

## Навигация по спринтам

### Sprint Selector

На Board и Metrics — переключатель спринтов:

```
[← Sprint 23]  [Sprint 24 ● active]  [Sprint 25 →]  [Все]
```

- Active спринт выбран по умолчанию
- "Все" — текущее Kanban-поведение (без фильтра по спринту)
- Closed спринты — для просмотра итогов
- Future спринты — для preview планирования

### URL

```
/board?teamId=1&sprintId=123
/board/metrics?teamId=1&sprintId=123
```

---

## Backend — архитектура

### Новые сервисы

```
SprintSyncService          — синхронизация спринтов из Jira Agile API
SprintService              — CRUD, текущий спринт, навигация
SprintMetricsService       — velocity, burndown, carry-over, scope change
SprintDigestService        — AI-генерация итогов (extends AI Digest)
SprintSnapshotService      — ежедневные snapshot для burndown
```

### Расширение существующих

```
SyncService                — вызывает SprintSyncService при methodology=SCRUM
UnifiedPlanningService     — учёт границ спринтов при methodology=SCRUM
BoardService               — фильтрация по sprint_id
TeamMetricsService         — sprint-scoped метрики
```

### JiraClient — новые методы

```java
// Список досок проекта
List<JiraBoard> getBoards(String projectKey);

// Спринты доски
List<JiraSprint> getSprints(long boardId, String state);

// Задачи в спринте
List<JiraIssue> getSprintIssues(long sprintId);
```

---

## Frontend — компоненты

### Новые

```
SprintBar.tsx              — полоса спринта на Board (goal, progress, capacity)
SprintSelector.tsx         — переключатель спринтов
SprintMetricsPanel.tsx     — метрики спринта (cards + burndown + velocity)
SprintDigest.tsx           — AI digest спринта
SprintBurndownChart.tsx    — SVG burndown chart
SprintVelocityChart.tsx    — SVG bar chart velocity по спринтам
```

### Расширение

```
TimelinePage.tsx           — sprint zone overlay
BoardPage.tsx              — sprint filter + SprintBar
TeamMetricsPage.tsx        — sprint tab/section
TeamMembersPage.tsx        — methodology selector
```

---

## Связь с другими фичами

| Фича | Связь |
|------|-------|
| **BF9** (Workflow Config) | methodology = часть team config; sprint board selection в wizard |
| **BF3** (Employee Dashboard) | Метрики сотрудника per sprint (фильтр по спринту) |
| **BF6** (AI Digest) | Sprint Digest = расширение AI Digest с sprint context |
| **BF7** (Notifications) | "Sprint ending in 1 day, 3 tasks incomplete" |
| **F14** (Timeline) | Sprint zone overlay на Gantt |
| **F21** (Unified Planning) | Sprint-aware scheduling |
| **F22** (Team Metrics) | Sprint-scoped metrics |

---

## Миграционный план

### Phase 1: Синхронизация + конфиг
1. Миграция: `methodology`, `jira_board_id` на `teams`; таблица `sprints`
2. JiraClient: методы для Agile API
3. SprintSyncService: синхронизация спринтов
4. API: список спринтов, текущий спринт

### Phase 2: UI визуализация
5. TeamMembersPage: methodology selector + board dropdown
6. BoardPage: SprintBar + Sprint filter
7. TimelinePage: sprint zone overlay
8. SprintSelector: переключатель спринтов

### Phase 3: Метрики
9. SprintSnapshotService: ежедневные snapshot
10. SprintMetricsService: velocity, carry-over, scope change
11. SprintBurndownChart: SVG burndown
12. SprintVelocityChart: SVG velocity trend
13. Sprint Metrics Panel: итоги спринта

### Phase 4: Digest + Planner
14. SprintDigestService: AI генерация итогов
15. UnifiedPlanningService: sprint-aware scheduling
16. Sprint capacity warnings

---

## Нефункциональные требования

- **Backward compatible:** Kanban-команды ничего не замечают. Все sprint-фичи скрыты при `methodology = KANBAN`
- **Jira Agile API:** Requires Jira Software (not Jira Work Management). Ошибку отображать: "Scrum mode requires Jira Software with Scrum board"
- **Performance:** Sprint sync добавляет 1-2 API вызова к стандартному sync cycle
- **Daily snapshots:** Cron job или при sync, не чаще раза в день

---

## Acceptance Criteria

1. Тимлид может переключить команду в режим Scrum и выбрать Jira-доску
2. Спринты синхронизируются автоматически (active, closed, future)
3. Board фильтрует задачи по спринту, показывает Sprint Bar с progress
4. Timeline отображает зоны спринтов (фон + лейблы)
5. Sprint Metrics показывают: velocity, burndown, carry-over, scope change
6. Sprint Digest генерирует AI-итог закрытого спринта
7. Автопланер учитывает границы спринтов и показывает overflow
8. Kanban-команды не затронуты — всё работает как раньше
