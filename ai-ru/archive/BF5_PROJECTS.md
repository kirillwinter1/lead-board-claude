# BF5. Projects (Project-Level Management)

## Обзор

Проект — задача типа **Project** в Jira, объединяющая эпики нескольких команд. Все данные (название, статус, assignee, даты) синхронизируются из Jira. Lead Board добавляет overlay: RICE-скоринг, прогресс, cross-team alignment и рекомендации.

---

## Иерархия задач (расширенная)

```
Project (тип задачи в Jira, имеет key, например LB-100)
└── Epic (связь через isChildOf/parent — конфигурируемо)
    └── Story / Bug
        └── Sub-task
```

- Проект = задача в Jira с `issueType = "Project"`
- У проекта есть Jira key (например `LB-100`), summary, status, assignee (PM), dueDate
- Один Epic → максимум один Project (или без проекта)
- Эпики разных команд (с разными или одинаковыми Jira project keys) могут входить в один проект

---

## Связь Project ↔ Epic (конфигурируемо)

Способ связи зависит от настроек Jira конкретной компании:

| Способ | Описание | Пример |
|--------|----------|--------|
| **parent** | Epic.parent = Project (стандартная иерархия Jira) | Epic LB-20 → parent: LB-100 |
| **issueLink** | Jira issue link типа isChildOf / isParentOf | LB-20 isChildOf LB-100 |

Настройка в конфигурации:

```yaml
project:
  epic-link-type: parent   # parent | issuelink
  issuelink-name: "is child of"  # название связи (если issuelink)
```

При синхронизации SyncService определяет связь Project→Epic по настроенному способу.

---

## Данные из Jira (синхронизация)

Проекты синхронизируются в `jira_issues` как обычные задачи с `issue_type = 'Project'`.

**Из Jira вытягиваем:**

| Поле | Источник | Описание |
|------|----------|----------|
| issueKey | Jira key | Например LB-100 |
| summary | Jira summary | Название проекта |
| status | Jira status | Статус (маппится на внутреннюю модель) |
| assignee | Jira assignee | Проектный менеджер |
| dueDate | Jira due date | Целевая дата |
| priority | Jira priority | Приоритет |
| created | Jira created | Дата создания |
| description | Jira description | Описание проекта (если нужно) |
| issueLinks | Jira links | Связи с эпиками (если epic-link-type=issuelink) |

**Локальные overlay-данные (не в Jira):**

| Поле | Описание |
|------|----------|
| RICE (reach, impact, confidence, effort, score) | Бизнес-ценность |
| Расчётный прогресс | Агрегация из эпиков |
| Expected Done | max(epicExpectedDone) |
| Cross-team recommendations | Генерируемые рекомендации |

---

## Status Model

Статусы проекта в Jira (настраиваются через StatusMappingService):

```
NEW → BUSINESS_REQUIREMENTS → DECOMPOSITION → PLANNING → IN_PROGRESS → ACCEPTANCE → DONE
```

| Статус Jira | Фаза | Описание |
|-------------|------|----------|
| NEW | Backlog | Проект создан |
| BUSINESS_REQUIREMENTS | Backlog | Составление БТ, оценка RICE |
| DECOMPOSITION | Backlog | Декомпозиция на эпики |
| PLANNING | TODO | Эпики созданы, оценены |
| IN_PROGRESS | In Progress | В работе |
| ACCEPTANCE | Review | Приёмка |
| DONE | Done | Завершён |

Маппинг через существующий `StatusMappingService` — добавляется тип задачи `Project`.

### Автоматические рекомендации по статусу

| Ситуация | Рекомендация |
|----------|-------------|
| Хотя бы 1 эпик в Developing, проект в PLANNING | "Рекомендуется перевести проект в IN_PROGRESS" |
| Все эпики Done, проект в IN_PROGRESS | "Все эпики завершены — можно перевести в ACCEPTANCE" |

---

## RICE Scoring

### Формула

```
RICE Score = (Reach × Impact × Confidence) / Effort
```

### Шкала значений

| Параметр | Значения |
|----------|---------|
| **Reach** | Число пользователей за период (free input) |
| **Impact** | Massive=3, High=2, Medium=1, Low=0.5, Minimal=0.25 |
| **Confidence** | High=1.0, Medium=0.8, Low=0.5 |
| **Effort** | Person-months (free input, > 0) |

### Хранение

RICE — локальные данные Lead Board (не в Jira). Хранятся в overlay-таблице `project_overlays` или как дополнительные поля в `jira_issues` (аналогично rough estimates).

### Влияние на AutoScore эпиков

Эпики внутри проекта получают бонус к AutoScore пропорционально RICE Score:

```
riceBoost = normalize(project.riceScore, minRice, maxRice) × MAX_RICE_WEIGHT
```

`MAX_RICE_WEIGHT` — конфигурируемый (по умолчанию 15 баллов).

---

## Cross-Team AutoScore Alignment

### Концепция

Когда проект содержит эпики от разных команд, система выравнивает приоритеты чтобы команды финишировали синхронно.

### Автоматический буст

1. Для каждого проекта рассчитывается "средняя дата завершения" эпиков
2. Эпики, отстающие от средней даты, получают буст AutoScore:
   ```
   alignmentBoost = delayDays × ALIGNMENT_WEIGHT_PER_DAY
   ```
3. Эпики, опережающие — без изменений (не штрафуются)

### Рекомендации

| Ситуация | Рекомендация |
|----------|-------------|
| Эпик отстаёт от проекта | "LB-30 (Team Beta) отстаёт от проекта LB-100 на N дней — рекомендуется поднять приоритет" |
| Все эпики завершены | "Все эпики проекта LB-100 завершены — можно перевести в Acceptance" |
| Эпик без forecast | "LB-40 (Team Gamma) не имеет forecast — невозможно рассчитать дату проекта" |
| RICE не заполнен | "Проект LB-100 в статусе Planning без RICE-оценки" |

---

## Расчёт прогресса и дат проекта

### Прогресс

```
projectProgress = sum(epicProgress × epicWeight) / sum(epicWeight)
```

Где `epicWeight` = effective estimate эпика (часы). Если нет оценок — равный вес.

Упрощённый вариант:
```
projectProgress = completedEpics / totalEpics × 100%
```

### Expected Done

```
projectExpectedDone = max(epic.expectedDone) по всем незавершённым эпикам
```

Если хотя бы один эпик без forecast → confidence = LOW.

---

## Роль Project Manager (PM)

### RBAC

Новая роль: `PROJECT_MANAGER`

**PROJECT_MANAGER и TEAM_LEAD — на одном уровне**, не подчинены друг другу:

```
ADMIN
├── PROJECT_MANAGER  (управление проектами)
├── TEAM_LEAD        (управление командами)
MEMBER
VIEWER
```

### Права PM

| Действие | PM | Team Lead |
|----------|-----|-----------|
| RICE-оценка проектов | ✅ | ❌ |
| Просмотр проектов и метрик | ✅ | ✅ |
| Просмотр всех команд и эпиков | ✅ | ✅ (свои) |
| Управление командами | ❌ | ✅ (свои) |
| Изменение приоритетов эпиков | ❌ | ✅ |
| Admin-функции | ❌ | ❌ |

**Примечание:** Создание/редактирование проекта и назначение PM происходит в Jira. Lead Board только синхронизирует.

### Будущее: Lead of Leads (концепт)

В будущем планируется роль "лид лидов" — TEAM_LEAD, управляющий другими TEAM_LEAD. Они составляют свою "команду" руководителей. Детали будут прорабатываться отдельно.

---

## Метрики проекта

Проект имеет собственные метрики (не агрегация из эпиков — это метрики самого проекта как сущности):

| Метрика | Описание |
|---------|----------|
| **DSR проекта** | Фактическое время от IN_PROGRESS до DONE (рабочие дни) / плановая длительность. Показывает насколько быстро проект доставлен относительно плана |
| **Отклонение от прогноза** | Forecast дата завершения vs фактическая дата. Дельта в днях (+опоздание, -раньше срока) |
| **Изменение оценки** | Как менялась суммарная оценка проекта со временем: initial estimate vs текущий estimate. Рост = scope creep |
| **Число багов** | Количество багов внутри эпиков проекта. Тренд по времени |
| **Время в статусах** | Сколько рабочих дней проект провёл в каждом статусе (NEW, BUSINESS_REQUIREMENTS, DECOMPOSITION, PLANNING, IN_PROGRESS, ACCEPTANCE). Через status_changelog |

Метрики доступны на странице проекта и используются в AI Digest (BF6) при закрытии проекта.

---

## UI

### Projects Page (`/projects`)

Отдельная вкладка в навигации.

#### Список проектов (свёрнутый вид)

```
┌──────────────────────────────────────────────────────────────────┐
│  ▶ LB-100 Редизайн Checkout Flow                    RICE: 120  │
│    PM: Иванов А.  │  In Progress  │  ████████░░ 72%            │
│    Эпиков: 3/4 завершено  │  Expected Done: 15 мар 2026        │
└──────────────────────────────────────────────────────────────────┘
```

Данные:
- Jira Key + Summary (expandable)
- PM (assignee из Jira, аватар + имя)
- Статус (бейдж с цветом)
- Прогресс (progress bar + %)
- RICE Score (локальный overlay)
- Эпиков: завершено / всего
- Expected Done (расчётная дата)
- Due Date delta (если есть dueDate в Jira)

#### Развёрнутый вид (эпики внутри проекта)

```
┌─ Эпик ────────────── Команда ──── Статус ────── Прогресс ── Expected ──┐
│  LB-10 Checkout UI   Team Alpha   ✅ Готово     ██████ 100%  28 янв   │
│  LB-20 Payment API   Team Alpha   🔄 Developing █████░  80%  20 фев   │
│  LB-30 Integ Tests   Team Beta    🔄 Developing ███░░░  55%  10 мар ⚠ │
│  LB-40 Analytics      Team Gamma  ⏳ Запланиров. ░░░░░░   0%  15 мар   │
├────────────────────────────────────────────────────────────────────────┤
│  ⚠ LB-30 отстаёт от плана проекта на 8 дней                          │
│  ℹ Все эпики Team Alpha на расписании                                 │
└────────────────────────────────────────────────────────────────────────┘
```

Колонки эпиков:
- Key + Summary
- Команда
- Статус (бейдж)
- Прогресс (bar + %)
- Expected Done
- Alert (⚠ если отстаёт)

**НЕ показываем** (в отличие от Board): разбивку по ролям SA/DEV/QA, subtasks, assignee — это детали уровня команды.

#### Фильтры

- По статусу проекта
- По PM
- Поиск по названию / key

#### Действия (локальные)

- Редактировать RICE (overlay)
- Просмотреть рекомендации

### Project Timeline (`/projects/timeline`)

Gantt-диаграмма на уровне проектов:

```
                    Янв         Фев         Мар         Апр
LB-100 Checkout     ████████████████████████████░░░░      |← due date
  LB-10 (Alpha)    ██████████████
  LB-20 (Alpha)         ████████████████
  LB-30 (Beta)               ██████████████████
  LB-40 (Gamma)                    ████████████████

LB-200 Mobile App                        ░░░░░░░░░░░░░░░░░░░░
```

- Строка проекта = от min(epicStart) до max(epicExpectedDone)
- Вложенные строки = эпики с указанием команды
- Цвет полосы = статус
- Due date = вертикальная линия

### Бейдж на Board

На существующем Board у эпиков, привязанных к проекту:

```
LB-20 Payment API  [📁 LB-100 Checkout]  Team Alpha  Developing...
```

---

## Архитектура Backend

### Синхронизация проектов

Проекты синхронизируются в существующий `jira_issues` как `issueType = 'Project'`.

**Изменения в SyncService:**
1. JQL включает тип Project: `project = {KEY} AND issuetype in (Epic, Story, Bug, Sub-task, Project)`
2. При обработке Project-задачи — определяем дочерние эпики по настроенному способу связи:
   - `parent`: ищем эпики где `parentKey = projectKey`
   - `issuelink`: парсим issueLinks, ищем связь isChildOf/isParentOf

### Пакет project

```
com.leadboard.project/
├── ProjectService             — Бизнес-логика (поиск проектов, дочерних эпиков)
├── ProjectProgressService     — Расчёт прогресса и expected done
├── ProjectAlignmentService    — Cross-team alignment (AutoScore boost + рекомендации)
├── ProjectRiceService         — RICE scoring (overlay данные)
├── ProjectController          — REST API
├── ProjectRecommendation      — DTO для рекомендаций
└── ProjectConfig              — Конфигурация (epic-link-type, etc.)
```

### DB Schema

```sql
-- Миграция: V__add_project_support.sql

-- RICE overlay для проектов (локальные данные, не из Jira)
CREATE TABLE project_overlays (
    id              BIGSERIAL PRIMARY KEY,
    issue_key       VARCHAR(50) NOT NULL UNIQUE,  -- ключ Project-задачи в Jira
    rice_reach      INTEGER,
    rice_impact     DECIMAL(4,2),        -- 0.25 to 3.0
    rice_confidence DECIMAL(3,2),        -- 0.0 to 1.0
    rice_effort     DECIMAL(10,2),       -- person-months
    rice_score      DECIMAL(10,2),       -- computed
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_project_overlays_key ON project_overlays(issue_key);
```

**Примечание:** Отдельная таблица `projects` НЕ нужна — проекты хранятся в `jira_issues`. Overlay-таблица только для локальных данных (RICE), аналогично rough estimates для эпиков.

### Определение связи Project → Epic

```java
// Конфигурация
@ConfigurationProperties(prefix = "project")
public class ProjectConfig {
    private String epicLinkType = "parent";     // parent | issuelink
    private String issueLinkName = "is child of"; // для issuelink
}

// Поиск эпиков проекта
public List<JiraIssueEntity> getProjectEpics(String projectKey) {
    if ("parent".equals(config.getEpicLinkType())) {
        // Эпики где parentKey = projectKey
        return issueRepository.findByParentKeyAndIssueType(projectKey, "Epic");
    } else {
        // Эпики связанные через issueLinks
        return issueRepository.findByIssueLinkTarget(projectKey, config.getIssueLinkName());
    }
}
```

### API Endpoints

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/projects` | Список проектов (фильтры: status, pmId) | Все |
| GET | `/api/projects/{key}` | Детали проекта с эпиками | Все |
| GET | `/api/projects/{key}/progress` | Прогресс + expected done | Все |
| GET | `/api/projects/{key}/recommendations` | Рекомендации alignment | Все |
| PUT | `/api/projects/{key}/rice` | Обновить RICE (overlay) | PM, Admin |
| GET | `/api/projects/timeline` | Данные для Gantt | Все |

**Примечание:** CRUD проектов нет — проекты создаются в Jira и синхронизируются. Lead Board только добавляет overlay.

### Frontend

```
frontend/src/
├── pages/
│   ├── ProjectsPage.tsx          — Список проектов
│   └── ProjectTimelinePage.tsx   — Gantt проектов
├── api/
│   └── projectApi.ts             — API клиент
└── components/
    └── projects/
        ├── ProjectCard.tsx        — Карточка проекта (свёрнутый вид)
        ├── ProjectEpicList.tsx    — Список эпиков (развёрнутый вид)
        ├── RiceForm.tsx           — RICE-форма (overlay)
        ├── ProjectProgress.tsx    — Progress bar + метрики
        └── ProjectRecommendations.tsx — Блок рекомендаций
```

Роутинг:
- `/projects` — список проектов
- `/projects/timeline` — Gantt

---

## План реализации (поэтапно)

### Этап 1: Синхронизация + UI списка
1. Конфигурация: `project.epic-link-type`, `project.issuelink-name`
2. SyncService: синхронизация задач типа Project
3. ProjectService: поиск проектов, определение дочерних эпиков
4. ProjectController: базовые endpoints
5. StatusMappingService: маппинг статусов Project
6. Frontend: ProjectsPage (список, развёрнутый вид с эпиками)
7. Тесты

### Этап 2: Прогресс + Expected Done
1. ProjectProgressService — агрегация из эпиков
2. Progress bar на ProjectCard
3. Expected Done расчёт (max из epicExpectedDone)
4. Бейдж проекта на Board
5. Тесты

### Этап 3: RICE Scoring + AutoScore влияние
1. DB миграция (project_overlays)
2. ProjectRiceService — расчёт RICE Score
3. RiceForm на UI
4. Интеграция RICE → AutoScore boost для эпиков проекта
5. Тесты

### Этап 4: Cross-team Alignment
1. ProjectAlignmentService — расчёт отставания/опережения
2. Автоматический AutoScore boost для отстающих эпиков
3. Генерация рекомендаций
4. UI: блок рекомендаций в развёрнутом проекте
5. Тесты

### Этап 5: Роль PM + Project Timeline
1. PROJECT_MANAGER в AppRole с правами
2. ProjectTimelinePage — Gantt
3. Данные для timeline (project bars + epic bars)
4. Due date линия
5. Тесты
