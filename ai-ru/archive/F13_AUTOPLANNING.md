# F13. Автопланирование (Auto Planning)

## Цель

Прогнозирование даты закрытия эпиков на основе:
- Остатка работы по эпику (estimate - logged)
- Команды, назначенной на эпик
- Других эпиков и задач команды
- Пропускной способности (capacity) команды
- Состава команды по ролям и грейдам

**Ключевая ценность:** Ответ на вопрос "Когда будет готово?" на основе фактов, а не обещаний.

---

## AutoScore (приоритет эпиков)

Автоматический расчёт приоритета эпика для планирования.

### Факторы (MVP)

| Фактор | Влияние |
|--------|---------|
| Статус | In Progress > Backlog |
| Прогресс | Чем ближе к завершению, тем выше |
| Due date из Jira | Чем ближе дедлайн, тем выше |
| Priority из Jira | Highest > High > Medium > Low |
| Размер эпика | Меньше = выше (быстрее закрыть) |
| Возраст | Старше = выше |
| Ручной override | Можно поднять приоритет в Lead Board |

### Факторы (будущее)

- Включение в квартал
- Зависимости (blocked/is blocked by)
- Баги приоритетнее фич
- Бизнесовые фичи > технические
- RICE scoring

---

## Расчёт прогноза

### Входные данные

1. **Остаток работы по эпику**
   - По ролям: SA, DEV, QA
   - Формула: `remaining = estimate - logged`

2. **Capacity команды**
   - Сумма `hoursPerDay` всех активных участников по ролям
   - Учёт коэффициентов грейдов

3. **Коэффициенты грейдов**
   - Senior: 0.8 (делает быстрее)
   - Middle: 1.0 (базовый)
   - Junior: 1.5 (делает медленнее)
   - Настраиваются в конфигурации команды

4. **Коэффициент рисков**
   - По умолчанию: 20%
   - Настраивается в конфигурации команды

5. **Производственный календарь**
   - Выходные (сб/вс)
   - Праздники РФ
   - Источник: внешний API (по умолчанию) или конфиг/БД (для закрытых контуров)

### Последовательность ролей

Строгая последовательность: **SA → DEV → QA**

Пока SA не завершил аналитику, DEV не может начать разработку.
Пока DEV не завершил разработку, QA не может начать тестирование.

### Распределение capacity

- Автоматическая оптимизация на основе AutoScore
- Алгоритм определяет оптимальное распределение ресурсов между эпиками
- Цель: минимизация общего времени закрытия с учётом приоритетов

### Параллельность

- Команда может работать над несколькими эпиками параллельно
- Разные роли могут работать над разными эпиками
- WIP лимиты ограничивают количество эпиков на роль

---

## WIP лимиты

### Настройка

- На уровне команды (общий лимит)
- По ролям (SA: 2, DEV: 3, QA: 2)

### Поведение

- При превышении — визуальная подсветка (не блокировка)
- Рекомендация: не более 2 эпиков на одного специалиста

---

## Флаги эпиков

- Источник: поле Flag из Jira
- Зафлаженный эпик выпадает из планирования будущей работы
- Залогированное время учитывается
- Подсветка в Board

---

## Колонка Expected Done

Отображает:
- **Прогнозная дата** (например, "15 марта")
- **Индикатор уверенности** (🟢 высокая / 🟡 средняя / 🔴 низкая)
- **Дельта от Due date** (+5 дней / -3 дня / в срок)

### Уровень уверенности

Факторы, влияющие на уверенность:
- Детализация (есть ли декомпозиция до sub-task)
- Исторические данные (точность прошлых оценок команды)
- Размер эпика (большие = больше неопределённости)
- Количество зависимостей
- Внешние зависимости
- Запланированные отпуска
- Нехватка ресурсов по ролям
- Частота смены приоритетов

---

## Timeline / Gantt

### Расположение

- Отдельная страница `/timeline`
- Переключатель в Board (Table ↔ Timeline)

### Детализация

- По умолчанию: эпики
- Раскрываемые: Story внутри эпика

### Интерактивность (будущее)

- Drag-drop для изменения приоритета
- Показ двух вариантов: автоматический AutoScore vs ручной override
- Версионирование для отката изменений

---

## Пересчёт прогноза

- По расписанию (scheduler, вместе с Jira sync)
- Вручную по кнопке "Recalculate"
- Кэширование результатов для производительности

---

## Производственный календарь

### Источники данных

| Режим | Источник |
|-------|----------|
| По умолчанию | Внешний API (xmlcalendar.ru / isdayoff.ru) |
| Закрытый контур | Конфигурационный файл |
| Закрытый контур | Таблица в БД |

### Настройка

- Источник настраивается в конфигурации
- Пока только РФ
- Будущее: страна привязана к пользователю

---

## Исторические данные

### Что сохраняем

1. **Финальные данные** — estimate vs actual по закрытым эпикам
2. **Снэпшоты прогнозов** — ежедневная фиксация Expected Done

### Отображение аналитики

- Отдельная страница `/analytics`
- Виджет на Dashboard
- История изменения прогноза в карточке эпика

---

## Конфигурация команды

Новые поля в настройках команды:

```yaml
planning:
  # Коэффициенты грейдов
  gradeCoefficients:
    senior: 0.8
    middle: 1.0
    junior: 1.5

  # Коэффициент рисков (добавляется к оценке)
  riskBuffer: 0.2

  # WIP лимиты
  wipLimits:
    team: 6        # Общий лимит на команду
    sa: 2          # Лимит на одного SA
    dev: 3         # Лимит на одного DEV
    qa: 2          # Лимит на одного QA
```

---

## Scope

### MVP (F13)

- ✅ AutoScore (базовые факторы: статус, прогресс, priority, due date, размер, возраст)
- ✅ Расчёт Expected Done с учётом capacity команды
- ✅ Колонка Expected Done в Board (дата + уверенность + дельта)
- ✅ Производственный календарь РФ (внешний API)
- ✅ Коэффициенты грейдов и рисков в настройках команды
- ✅ Последовательность SA → DEV → QA
- ✅ Ручной override приоритета

### Отложено (F13.x)

- ⏳ Timeline/Gantt визуализация
- ⏳ Drag-drop приоритетов с версионированием
- ⏳ WIP лимиты с подсветкой
- ⏳ Снэпшоты и аналитика точности
- ⏳ Зависимости между эпиками (blocked/is blocked by)
- ⏳ Флаги из Jira
- ⏳ Отпуска и отсутствия сотрудников
- ⏳ Выбор страны для календаря

---

## API (проектируемый)

### Получение прогноза

```
GET /api/planning/forecast
Response: {
  "calculatedAt": "2026-01-23T12:00:00Z",
  "epics": [
    {
      "epicKey": "LB-123",
      "autoScore": 85.5,
      "manualPriorityBoost": 0,
      "expectedDone": "2026-03-15",
      "confidence": "high",
      "dueDateDelta": -3,
      "remainingByRole": {
        "sa": { "hours": 16, "days": 2.0 },
        "dev": { "hours": 80, "days": 10.0 },
        "qa": { "hours": 24, "days": 3.0 }
      }
    }
  ]
}
```

### Обновление приоритета

```
PATCH /api/planning/epics/{epicKey}/priority
Request: { "boost": 10 }
```

### Пересчёт прогноза

```
POST /api/planning/recalculate
```

### Конфигурация планирования команды

```
GET /api/teams/{id}/planning-config
PUT /api/teams/{id}/planning-config
```

### Производственный календарь

```
GET /api/calendar/workdays?from=2026-01-01&to=2026-03-31
Response: {
  "workdays": ["2026-01-09", "2026-01-10", ...],
  "holidays": [
    { "date": "2026-01-01", "name": "Новый год" },
    ...
  ]
}
```

---

## Зависимости

- F3 (Sync & Cache) — данные из Jira
- F5 (Team Backend) — состав команды, роли, грейды
- F10 (Epic-Team Mapping) — связь эпика с командой
- F11 (Rough Estimates) — оценки для эпиков без декомпозиции
