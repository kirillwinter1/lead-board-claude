# BF11. Competency Matrix (Компетенции сотрудников)

## Обзор

Матрица компетенций сотрудников по двум измерениям: **компоненты** (технические области) и **продукты** (бизнес-линии). Данные подтягиваются из Jira (Component, Product custom field). Тимлид заполняет уровень экспертности каждого сотрудника на странице профиля.

Компетенции влияют на **автопланер**: выбор оптимального исполнителя и расчёт скорости выполнения задачи (`effectiveHours = estimate × gradeCoeff × competencyFactor`).

---

## Проблема

Сейчас автопланер знает о сотруднике:
- **Роль** (DEV/QA/SA) — что может делать
- **Грейд** (Senior/Middle/Junior) — коэффициент скорости
- **Capacity** (hoursPerDay) — сколько часов в день

Но **не знает** предметную экспертизу:
- Иванов (DEV) отлично знает sync-service и Кредитование
- Петров (DEV) отлично знает sync-service и Страхование
- Задача "Fix sync для Кредитования" назначается на Петрова (он свободнее), хотя Иванов сделал бы в 2 раза быстрее

**Результат:** неоптимальное распределение, завышенные сроки в forecast, bus factor.

---

## Модель компетенций

### Два измерения

```
                    Продукты (доменная экспертиза)
                    ┌──────────┬──────────┬──────────┐
                    │Кредитов. │Страхов.  │Инвестиции│
┌───────────────┬───┼──────────┼──────────┼──────────┤
│ sync-service  │   │ Иванов=5 │ Петров=5 │    —     │
│               │ К │          │          │          │
├───────────────┤ о ├──────────┼──────────┼──────────┤
│ auth-module   │ м │ Иванов=3 │ Петров=3 │Сидоров=4 │
│               │ п ├──────────┼──────────┼──────────┤
├───────────────┤ о │          │          │          │
│ frontend      │ н │Сидоров=4 │Сидоров=4 │Сидоров=4 │
│               │ е ├──────────┼──────────┼──────────┤
├───────────────┤ н │          │          │          │
│ payment-gw    │ т │    —     │    —     │Сидоров=5 │
│               │ ы │          │          │          │
└───────────────┴───┴──────────┴──────────┴──────────┘
```

### Источники данных

| Измерение | Источник в Jira | Как получить |
|-----------|----------------|-------------|
| **Компонент** (техническая область) | Встроенное поле `Component` | `GET /rest/api/3/project/{key}/components` |
| **Продукт** (бизнес-линия) | Custom field (Select List) | Настраивается в конфигурации: какое Jira-поле = "Продукт" |

Оба списка подтягиваются из Jira автоматически. Тимлид не вводит названия вручную — только выставляет уровни.

### Шкала компетенций (5 уровней)

| Уровень | Название | Описание | Competency Factor |
|---------|----------|----------|-------------------|
| **5** | Expert | Глубокие знания, менторит других | 0.7 (на 30% быстрее) |
| **4** | Proficient | Работает самостоятельно, решает сложные кейсы | 0.85 |
| **3** | Competent | Может работать, иногда нужна помощь (дефолт) | 1.0 (baseline) |
| **2** | Beginner | Нужно наставничество, базовые задачи | 1.3 (на 30% медленнее) |
| **1** | No experience | Не работал с этой областью | 1.6 (на 60% медленнее) |

**Дефолт:** если у сотрудника не заполнена компетенция по области → считается **3 (Competent)**.

---

## Расчёт Competency Score для задачи

### Формула

Каждая задача в Jira имеет (опционально) component(s) и product. Для каждого кандидата считаем:

```
componentScore = avg(уровни по компонентам задачи)   // или 3 если не заполнено
productScore   = уровень по продукту задачи           // или 3 если не заполнено

competencyScore = (componentScore + productScore) / 2  // равный вес 50/50
```

### Пример

Задача LB-201: component=`sync-service`, product=`Кредитование`

```
Иванов:   sync-service=4, Кредитование=5  → (4+5)/2 = 4.5  ← лучший
Петров:   sync-service=4, Кредитование=1  → (4+1)/2 = 2.5
Сидоров:  sync-service=3, Кредитование=3  → (3+3)/2 = 3.0  (дефолт — не заполнено)
```

### Несколько компонентов

Если задача имеет 2+ components:
```
Задача: components=[sync-service, auth-module], product=Кредитование
Иванов: sync-service=4, auth-module=3, Кредитование=5
→ componentScore = avg(4, 3) = 3.5
→ competencyScore = (3.5 + 5) / 2 = 4.25
```

### Задача без компонента или продукта

| Component | Product | Расчёт |
|-----------|---------|--------|
| Есть | Есть | `(componentScore + productScore) / 2` |
| Есть | Нет | `componentScore` (только техническая ось) |
| Нет | Есть | `productScore` (только доменная ось) |
| Нет | Нет | `3.0` (дефолт, компетенция не учитывается) |

---

## Влияние на автопланер

### 1. Выбор исполнителя

При назначении subtask на конкретного сотрудника автопланер учитывает:

```
Текущий алгоритм (UnifiedPlanningService):
  Для каждого subtask → выбрать сотрудника с нужной ролью и наименьшей загрузкой

Новый алгоритм:
  Для каждого subtask:
    1. Кандидаты = сотрудники с нужной ролью
    2. Для каждого кандидата:
       a. availableDate = когда освободится (текущая логика)
       b. competencyScore = расчёт по component + product задачи
       c. effectiveHours = estimate × gradeCoeff × competencyFactor(score)
       d. endDate = availableDate + effectiveHours / hoursPerDay
    3. Выбрать кандидата с наименьшей endDate
```

Это значит: если эксперт занят ещё 1 день, а новичок свободен сейчас, но ему нужно в 2 раза больше времени → система может выбрать эксперта (зависит от конкретных чисел).

### 2. Расчёт скорости (effectiveHours)

```java
// Было:
effectiveHours = estimate × gradeCoefficient

// Стало:
competencyFactor = getCompetencyFactor(competencyScore);  // 0.7 ... 1.6
effectiveHours = estimate × gradeCoefficient × competencyFactor
```

**Таблица competencyFactor:**

| Score | Factor | Интерпретация |
|-------|--------|---------------|
| 5.0 | 0.70 | Expert — на 30% быстрее |
| 4.5 | 0.78 | |
| 4.0 | 0.85 | Proficient |
| 3.5 | 0.93 | |
| 3.0 | 1.00 | Competent (baseline, дефолт) |
| 2.5 | 1.15 | |
| 2.0 | 1.30 | Beginner — на 30% медленнее |
| 1.5 | 1.45 | |
| 1.0 | 1.60 | No experience — на 60% медленнее |

Линейная интерполяция между уровнями.

```java
public static double getCompetencyFactor(double score) {
    // Линейная интерполяция: score 1→factor 1.6, score 5→factor 0.7
    return 1.6 - (score - 1.0) * (1.6 - 0.7) / (5.0 - 1.0);
    // = 1.6 - (score - 1.0) * 0.225
}
```

### 3. Влияние на forecast

Forecast эпиков и stories станет точнее: если задача назначена на эксперта → effectiveHours меньше → expectedDone раньше. И наоборот для новичков.

---

## UI: Страница сотрудника

### Текущая страница TeamMembersPage

Сейчас показывает: имя, роль, грейд, hoursPerDay. Добавляем секцию "Компетенции".

### Расширенная страница

```
┌─ Иванов Алексей ─────────────────────────────────────────────┐
│                                                               │
│  Роль: DEV    Грейд: Senior    Часов/день: 8                │
│                                                               │
│  ─── Компоненты (техническая экспертиза) ─────────────────   │
│                                                               │
│  sync-service       ●●●●○  4/5  Proficient     [✏️]         │
│  auth-module        ●●○○○  2/5  Beginner       [✏️]         │
│  frontend           ●○○○○  1/5  No experience  [✏️]         │
│  payment-gateway    ●●●○○  3/5  Competent      [✏️]         │
│                                                               │
│  Нет оценки: notification-service, reporting-engine          │
│                                            [Оценить]         │
│                                                               │
│  ─── Продукты (доменная экспертиза) ──────────────────────   │
│                                                               │
│  Кредитование       ●●●●●  5/5  Expert         [✏️]         │
│  Страхование        ●○○○○  1/5  No experience  [✏️]         │
│  Инвестиции         ●●●○○  3/5  Competent      [✏️]         │
│                                                               │
│  ─── Bus Factor ──────────────────────────────────────────   │
│                                                               │
│  ⚠️ Единственный Expert по: sync-service + Кредитование      │
│  ℹ️ Рекомендация: прокачать Петрова по Кредитованию          │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

### Редактирование компетенции

Клик на `[✏️]` или на шкалу → inline edit:

```
sync-service    [1 ○] [2 ○] [3 ○] [4 ●] [5 ○]    Proficient
                 No    Beg   Comp  Prof  Expert
```

Или клик на точки (●●●●○) — интуитивно как рейтинг.

### Список незаполненных

Компоненты и продукты из Jira, для которых нет оценки у этого сотрудника, показываются отдельно с кнопкой "Оценить". Это помогает тимлиду не забыть заполнить.

---

## UI: Обзор компетенций команды

Отдельная страница или вкладка — матрица всей команды:

```
┌─ Компетенции команды Alpha ──────────────────────────────────────────┐
│                                                                       │
│  [Компоненты ▼]                                                      │
│                                                                       │
│                   Иванов    Петров    Сидоров    Козлов               │
│                   DEV Sr    DEV Mid   DEV Jr     QA Mid              │
│  ────────────────────────────────────────────────────────            │
│  sync-service     ●●●●○ 4   ●●●●○ 4   ●●●○○ 3    ●●○○○ 2           │
│  auth-module      ●●○○○ 2   ●●●○○ 3   ●○○○○ 1    ●●●○○ 3           │
│  frontend         ●○○○○ 1   ●●●○○ 3   ●●●●○ 4    ●○○○○ 1           │
│  payment-gw       ●●●○○ 3   ●○○○○ 1   ●○○○○ 1    ●●●●●5            │
│                                                                       │
│  ─── Bus Factor Alert ────────────────────────────────────           │
│  🔴 payment-gw: только Козлов (QA) — нет DEV с экспертизой!         │
│  ⚠️ sync-service: только Иванов и Петров — 0 backup                  │
│  ✅ frontend: Петров + Сидоров — покрытие достаточное                 │
│                                                                       │
│  [Продукты ▼]                                                        │
│                                                                       │
│                   Иванов    Петров    Сидоров    Козлов               │
│  ────────────────────────────────────────────────────────            │
│  Кредитование     ●●●●● 5   ●○○○○ 1   ●●●○○ 3    ●●●○○ 3           │
│  Страхование      ●○○○○ 1   ●●●●● 5   ●●○○○ 2    ●●●○○ 3           │
│  Инвестиции       ●●●○○ 3   ●●○○○ 2   ●●●●○ 4    ●●○○○ 2           │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

---

## Bus Factor

### Расчёт

Для каждой области (компонент или продукт) считаем количество сотрудников с уровнем ≥ 4 (Proficient+):

| Experts (≥4) | Уровень | Описание |
|-------------|---------|----------|
| 0 | 🔴 CRITICAL | Никто не знает область — критический риск |
| 1 | ⚠️ WARNING | Единственный эксперт — bus factor = 1 |
| 2 | ✅ OK | Есть backup |
| 3+ | ✅ GOOD | Хорошее покрытие |

### Алерты

```
🔴 payment-gateway: 0 DEV с экспертизой ≥4 — нет кому делать сложные задачи
⚠️ sync-service + Кредитование: только Иванов — bus factor = 1
ℹ️ Рекомендация: назначать Петрова на задачи sync-service+Кредитование для прокачки
```

### Bus Factor по пересечению (Компонент × Продукт)

Самый ценный bus factor — по пересечению:
```
sync-service + Кредитование: [Иванов=4.5] — только 1 человек
sync-service + Страхование:  [Петров=4.5] — только 1 человек
auth-module + Инвестиции:    [Сидоров=4]  — только 1 человек
```

---

## Конфигурация Jira-поля "Продукт"

### Настройка

В настройках проекта (BF9 или отдельная настройка) админ указывает какое Jira custom field содержит продукт/бизнес-линию:

```
┌─ Настройки компетенций ──────────────────────────────────────┐
│                                                               │
│  Поле "Компонент":   Component (встроенное)  ✅               │
│                                                               │
│  Поле "Продукт":     [customfield_12345 ▼]                  │
│                       "Product Line"                          │
│                                                               │
│  Доступные значения (из Jira):                               │
│    ✅ Кредитование                                            │
│    ✅ Страхование                                             │
│    ✅ Инвестиции                                               │
│    ☐ Внутренний (не использовать)                             │
│                                                               │
│                                               [Сохранить]    │
└───────────────────────────────────────────────────────────────┘
```

### API для получения значений

```
GET /rest/api/3/project/{key}/components
→ [{ id: "10001", name: "sync-service" }, ...]

GET /rest/api/3/field/customfield_12345/context/{contextId}/option
→ [{ id: "10101", value: "Кредитование" }, ...]
```

---

## Синхронизация

### При sync задач

`SyncService` уже подтягивает задачи из Jira. Добавляем:
- Извлечение `components[]` из задачи → сохранение в `jira_issues`
- Извлечение значения product custom field → сохранение в `jira_issues`

```sql
ALTER TABLE jira_issues ADD COLUMN components TEXT[];       -- ["sync-service", "auth-module"]
ALTER TABLE jira_issues ADD COLUMN product VARCHAR(200);    -- "Кредитование"
```

### При sync метаданных

`TrackerProvider.fetchComponents()` — список компонентов проекта
`TrackerProvider.fetchProductFieldOptions()` — значения поля "Продукт"

---

## DB Schema

```sql
-- Миграция: V__create_competency_matrix.sql

-- ============================================
-- 1. Компетенции сотрудника по компонентам
-- ============================================

CREATE TABLE member_component_competencies (
    id              BIGSERIAL PRIMARY KEY,
    team_member_id  BIGINT NOT NULL REFERENCES team_members(id) ON DELETE CASCADE,
    component_name  VARCHAR(200) NOT NULL,      -- "sync-service", "auth-module"
    level           INTEGER NOT NULL DEFAULT 3,  -- 1-5
    updated_at      TIMESTAMP DEFAULT NOW(),
    updated_by      BIGINT,                      -- user_id кто выставил
    UNIQUE(team_member_id, component_name),
    CHECK(level >= 1 AND level <= 5)
);

-- ============================================
-- 2. Компетенции сотрудника по продуктам
-- ============================================

CREATE TABLE member_product_competencies (
    id              BIGSERIAL PRIMARY KEY,
    team_member_id  BIGINT NOT NULL REFERENCES team_members(id) ON DELETE CASCADE,
    product_name    VARCHAR(200) NOT NULL,       -- "Кредитование", "Страхование"
    level           INTEGER NOT NULL DEFAULT 3,  -- 1-5
    updated_at      TIMESTAMP DEFAULT NOW(),
    updated_by      BIGINT,                      -- user_id кто выставил
    UNIQUE(team_member_id, product_name),
    CHECK(level >= 1 AND level <= 5)
);

-- ============================================
-- 3. Настройка поля "Продукт" в Jira
-- ============================================
-- Хранится в project_configurations (BF9) как доп. поле:

ALTER TABLE project_configurations ADD COLUMN product_field_id VARCHAR(100);
  -- "customfield_12345" — ID custom field в Jira, содержащего продукт

-- ============================================
-- 4. Расширение jira_issues
-- ============================================

ALTER TABLE jira_issues ADD COLUMN components TEXT[];
ALTER TABLE jira_issues ADD COLUMN product VARCHAR(200);

CREATE INDEX idx_jira_issues_components ON jira_issues USING GIN(components);
CREATE INDEX idx_jira_issues_product ON jira_issues(product);

-- ============================================
-- Индексы компетенций
-- ============================================

CREATE INDEX idx_comp_competencies_member ON member_component_competencies(team_member_id);
CREATE INDEX idx_prod_competencies_member ON member_product_competencies(team_member_id);
```

---

## Архитектура

### Backend

```
com.leadboard.competency/
├── entity/
│   ├── ComponentCompetencyEntity      — Компетенция по компоненту
│   └── ProductCompetencyEntity        — Компетенция по продукту
├── repository/
│   ├── ComponentCompetencyRepository
│   └── ProductCompetencyRepository
├── service/
│   ├── CompetencyService              — CRUD компетенций, расчёт score
│   ├── CompetencyScoreCalculator      — Расчёт competencyScore и competencyFactor
│   └── BusFactorService               — Расчёт bus factor и алертов
├── controller/
│   └── CompetencyController           — REST API
└── dto/
    ├── CompetencyLevelDto             — Уровень компетенции {area, level}
    ├── MemberCompetencyDto            — Все компетенции сотрудника
    ├── TeamCompetencyMatrixDto        — Матрица команды
    ├── CompetencyScoreDto             — Score кандидата для задачи
    └── BusFactorDto                   — Bus factor алерты
```

### Интеграция с автопланером

```
com.leadboard.planning/
└── UnifiedPlanningService
    └── assignSubtask()
        ├── getCandidates(role)              — существующая логика
        ├── calculateAvailableDate()         — существующая логика
        ├── competencyScore = CompetencyScoreCalculator
        │     .calculate(candidate, task.components, task.product)
        ├── competencyFactor = getCompetencyFactor(competencyScore)
        ├── effectiveHours = estimate × gradeCoeff × competencyFactor
        └── endDate = availableDate + effectiveHours / hoursPerDay
            → выбрать кандидата с min(endDate)
```

### Frontend

```
frontend/src/
├── components/
│   └── competency/
│       ├── CompetencyRating.tsx           — Шкала ●●●●○ с inline edit
│       ├── MemberCompetencySection.tsx    — Секция компетенций на странице сотрудника
│       ├── TeamCompetencyMatrix.tsx       — Обзор компетенций команды (таблица)
│       ├── BusFactorAlerts.tsx            — Алерты bus factor
│       └── CompetencyConfigSettings.tsx   — Настройка поля "Продукт"
├── api/
│   └── competencyApi.ts                   — API клиент
└── hooks/
    └── useCompetencies.ts                 — Хук для загрузки компетенций
```

---

## API Endpoints

### Компетенции сотрудника

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/competencies/member/{memberId}` | Все компетенции сотрудника | TeamLead+ |
| PUT | `/api/competencies/member/{memberId}/components` | Обновить компетенции по компонентам `[{name, level}]` | TeamLead+ |
| PUT | `/api/competencies/member/{memberId}/products` | Обновить компетенции по продуктам `[{name, level}]` | TeamLead+ |
| PUT | `/api/competencies/member/{memberId}/component/{name}` | Обновить один уровень `{level}` | TeamLead+ |
| PUT | `/api/competencies/member/{memberId}/product/{name}` | Обновить один уровень `{level}` | TeamLead+ |

### Матрица команды

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/competencies/team/{teamId}` | Полная матрица команды (все сотрудники × все области) | TeamLead+ |
| GET | `/api/competencies/team/{teamId}/bus-factor` | Bus factor алерты | TeamLead+ |

### Score для задачи

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/competencies/score?issueId={id}` | Competency score всех кандидатов для задачи | TeamLead+ |

### Настройки

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/api/competencies/config/{projectKey}` | Настройка поля "Продукт" | Admin |
| PUT | `/api/competencies/config/{projectKey}` | Обновить настройку `{productFieldId}` | Admin |
| GET | `/api/competencies/areas/{projectKey}` | Доступные компоненты и продукты (из Jira) | TeamLead+ |

---

## Безопасность

- **Просмотр компетенций:** TeamLead+ (своя команда), Admin (все)
- **Редактирование компетенций:** TeamLead+ (своя команда)
- **Настройка поля "Продукт":** Admin
- **Audit:** изменения компетенций логируются (updated_by, updated_at)

---

## Зависимости

| Зависимость | Тип | Описание |
|-------------|-----|----------|
| BF9 Workflow Configuration | Требуется | Конфигурация проекта, TrackerProvider для получения компонентов и custom fields |
| F27 RBAC | Требуется | Доступ TeamLead/Admin |
| UnifiedPlanningService | Модифицируется | Добавляется competencyFactor в расчёт effectiveHours |
| SyncService | Модифицируется | Подтягивание components и product из задач Jira |

---

## План реализации (поэтапно)

### Этап 1: DB + Sync + API

1. Flyway-миграции: `member_component_competencies`, `member_product_competencies`
2. Flyway-миграция: `ALTER TABLE jira_issues ADD components, product`
3. Flyway-миграция: `ALTER TABLE project_configurations ADD product_field_id`
4. JPA entities, repositories
5. Расширение SyncService: извлечение components и product из Jira-задач
6. `CompetencyService` — CRUD компетенций
7. `CompetencyController` — API
8. Тесты

### Этап 2: Расчёт Score + Интеграция с автопланером

1. `CompetencyScoreCalculator` — расчёт score по формуле
2. `getCompetencyFactor()` — линейная интерполяция score → factor
3. Модификация `UnifiedPlanningService.assignSubtask()` — учёт competencyFactor
4. Модификация расчёта effectiveHours
5. Тесты (проверить что при дефолте 3/3 → factor=1.0 → ничего не меняется)

### Этап 3: Bus Factor

1. `BusFactorService` — расчёт покрытия по областям
2. Bus factor по пересечению (компонент × продукт)
3. API endpoint: bus-factor алерты
4. Тесты

### Этап 4: Frontend — Страница сотрудника

1. `CompetencyRating` — компонент шкалы ●●●●○ с inline edit
2. `MemberCompetencySection` — секция на странице сотрудника
3. Список незаполненных областей с кнопкой "Оценить"
4. `competencyApi.ts` — API клиент
5. Интеграция в существующую `TeamMembersPage`

### Этап 5: Frontend — Обзор команды + Bus Factor

1. `TeamCompetencyMatrix` — таблица всей команды
2. Переключение: Компоненты / Продукты
3. `BusFactorAlerts` — алерты
4. `CompetencyConfigSettings` — настройка поля "Продукт"

### Этап 6: Интеграция с BF10 (Eisenhower Matrix)

1. В рекомендациях автопланера учитывать competencyScore
2. При показе кандидатов для задачи → отображать score
3. Тесты

---

## Будущие расширения

| Расширение | Когда |
|------------|-------|
| Автоматический расчёт компетенций из истории (кто сколько часов работал с компонентом/продуктом) | После MVP |
| Режим "Knowledge Sharing" — намеренно назначать на менее опытных для прокачки | После MVP |
| Тренды компетенций — как меняются уровни со временем | После стабилизации |
| Рекомендации по прокачке: "Назначьте Петрова на задачи Кредитования для роста компетенции" | После Bus Factor |
| Визуализация: radar chart компетенций сотрудника | После MVP |
| Интеграция с Planning Poker — показывать competency score при оценке | После MVP |
