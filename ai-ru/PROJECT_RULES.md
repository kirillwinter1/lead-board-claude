# Правила и ограничения проекта Lead Board

## КРИТИЧНЫЕ ПРАВИЛА (применяются ко всему проекту!)

### 1. Оценки и логирование времени ТОЛЬКО в Subtasks

**ПРАВИЛО:** Все оценки (Original Estimate) и залогированное время (Time Spent) хранятся ТОЛЬКО на уровне **Subtask**.

**Следствия:**
- Story БЕЗ subtasks с estimates = НЕ МОЖЕТ быть запланирована
- Estimate и Time Spent на уровне Story игнорируются
- Все расчёты прогресса, remaining work, Expected Done агрегируются из subtasks
- При отображении Story показываем сумму из всех её subtasks

**Пример:**
```
Epic LB-100
  ├─ Story LB-101 (estimate игнорируется)
  │   ├─ Subtask LB-102 "Аналитика" (estimate: 8h, logged: 6h) ✅
  │   └─ Subtask LB-103 "Разработка" (estimate: 16h, logged: 10h) ✅
  └─ Story LB-104 (БЕЗ subtasks) ❌ НЕ ПЛАНИРУЕТСЯ
```

---

### 2. Assignee ТОЛЬКО в Subtasks

**ПРАВИЛО:** Assignee (исполнитель) назначается ТОЛЬКО на **Subtask**.

**Следствия:**
- Story без assigned subtasks = нельзя определить кто работает
- При расчёте capacity и планирования смотрим на assignee subtasks
- В Timeline показываем кто работает на основе subtasks
- Assignee на уровне Story игнорируется

**Важно для планирования:**
- Знаем КАКОЙ человек в КАКОЙ период времени делает ТУ или ИНУЮ работу
- Планирование учитывает capacity конкретных людей через их subtasks
- История работы отслеживается по subtasks

---

### 3. Story без Subtasks с Estimates = не планируется

**ПРАВИЛО:** Story, у которой нет subtasks с estimates, считается некачественно декомпозированной и:
- Выпадает из планирования (не участвует в расчёте Expected Done)
- Помещается в КОНЕЦ списка stories внутри epic
- Подсвечивается в UI как проблемная (⚠️ No subtask estimates)

**Data Quality правило:**
- `STORY_NO_SUBTASK_ESTIMATES` (WARNING) - Story has no subtasks with estimates

---

### 4. Data Quality: Subtask в активном статусе, но Story не в работе

**ПРАВИЛО:** Если subtask в статусе `In Progress` или `Review`, но parent story в статусе `New`, `Ready`, или других TODO статусах - это нарушение workflow.

**Поведение:**
- Создаётся Data Quality violation: `SUBTASK_ACTIVE_STORY_NOT_INPROGRESS` (WARNING)
- Story подсвечивается в UI (⚠️ Subtask active but story not in progress)
- НЕ блокирует планирование (время учитывается, но есть предупреждение)

---

## Workflow статусы

### Story Workflow

| Статус | Категория | Вес для AutoScore | Фаза |
|--------|-----------|-------------------|------|
| New | TODO | 10 | - |
| Ready | TODO | 20 | - |
| Waiting Dev | TODO | 30 | - |
| Waiting QA | TODO | 40 | - |
| Ready to Release | TODO | 50 | - |
| Analysis | IN_PROGRESS | 60 | SA |
| Analysis Review | IN_PROGRESS | 70 | SA |
| Development | IN_PROGRESS | 80 | DEV |
| Dev Review | IN_PROGRESS | 90 | DEV |
| Testing | IN_PROGRESS | 100 | QA |
| Test Review | IN_PROGRESS | 110 | QA |
| Done | DONE | 0 (не планируется) | - |

**Русские эквиваленты:**
- Новый, Готов, Ожидает разработки, Ожидает тестирования, Готов к релизу (TODO)
- Анализ, Ревью анализа, Разработка, Ревью разработки, Тестирование, Ревью тестирования (IN_PROGRESS)
- Готово (DONE)

### Subtask Workflow

| Статус | Категория |
|--------|-----------|
| New, Новый | TODO |
| In Progress, Review, В работе, Ревью | IN_PROGRESS |
| Done, Готово | DONE |

---

## Issue Types

### Приоритет по типу задачи

1. **Bug** (Баг, Дефект) - высший приоритет
2. **Story** (История) - стандартный приоритет

---

## Jira Priority

Учитываем Jira Priority:
- **Highest** - наивысший приоритет
- **High** - высокий
- **Medium** - средний
- **Low** - низкий

---

## Флаги (Flagged)

**ПРАВИЛО:** Flagged story означает, что работа по ней НЕ ведется.

**Поведение:**
- Понижаем приоритет (в конец списка)
- Не включаем в активное планирование
- Подсвечиваем в UI (🚩 Flagged - work paused)

---

## Блокировки (Dependencies)

### Jira Link Types: "is blocked by" / "blocks"

**Story A blocks Story B:**
- A должна быть выполнена раньше B
- A получает повышенный приоритет (нужно сделать раньше, чтобы разблокировать другие)
- B понижается в приоритете (не может начаться пока A не выполнена)

**Story X is blocked by Story Y:**
- X не может начаться пока Y не выполнена
- X откладывается в планировании
- X подсвечивается в UI (🔒 Blocked by Y)

### Топологическая сортировка

При наличии зависимостей:
1. Строим граф dependencies
2. Топологическая сортировка (blocked stories автоматически после blocking)
3. Внутри "слоёв" без зависимостей - сортировка по AutoScore

### Data Quality правила для зависимостей

- `STORY_BLOCKED_BY_MISSING` (ERROR) - Story blocked by несуществующую story
- `STORY_CIRCULAR_DEPENDENCY` (ERROR) - Циклическая зависимость (A blocks B blocks A)
- `STORY_BLOCKED_NO_PROGRESS` (WARNING) - Story заблокирована более 30 дней без прогресса

---

## Агрегация данных из Subtasks

### Estimate и Time Spent

```
Story Total Estimate = SUM(subtask.originalEstimateSeconds WHERE subtask NOT DONE)
Story Total Logged = SUM(subtask.timeSpentSeconds)
Story Remaining = Story Total Estimate - Story Total Logged
Story Progress = Story Total Logged / Story Total Estimate (если estimate > 0)
```

### Role Breakdown

Subtasks группируются по ролям (SA/DEV/QA) на основе:
1. Issue Type (приоритет)
2. Status
3. Assignee role (если доступно из Team Members)

```
SA Estimate = SUM(subtask.estimate WHERE role=SA)
DEV Estimate = SUM(subtask.estimate WHERE role=DEV)
QA Estimate = SUM(subtask.estimate WHERE role=QA)
```

---

## Планирование и Timeline

### Последовательность фаз

Строгая последовательность: **SA → DEV → QA**

### Учёт capacity

Capacity берётся из Team Members (hoursPerDay × grade coefficient)

### StoryDuration

Настраивается в planning config команды (по умолчанию 1-2 дня на story по каждой роли).

Влияет на конвейерную модель (меньше duration = больше параллелизм фаз).

---

## Ручное управление

### Manual Priority Boost

- Пользователь может вручную повысить/понизить приоритет story и epic
- Значение сохраняется в БД (поле `manualPriorityBoost`)
- Добавляется к AutoScore при сортировке
- **Нет ограничений** - можно задать любое число (положительное или отрицательное)

### Drag & Drop

- В Board можно перетаскивать stories внутри epic для изменения порядка
- При drag & drop пересчитывается `manualPriorityBoost`

---

## Пересчёт AutoScore

**Триггеры пересчёта:**
- После каждой Jira sync (автоматически)
- По кнопке "Recalculate" в UI (вручную)
- При изменении manual boost (автоматически)

**Scope пересчёта:**
- Epic AutoScore пересчитывается всегда
- Story AutoScore пересчитывается для всех stories всех epics команды

---

## UI Индикаторы

### Иконки

- 🐛 Bug
- 🔒 Blocked
- 🚩 Flagged
- ⚠️ No subtask estimates
- ⚡ High AutoScore (>80)

### Цвета

- **SA** - синий (#0052cc)
- **DEV** - зелёный (#36b37e)
- **QA** - оранжевый (#ff991f)

### Подсветка проблем

- Красная рамка - ERROR violations
- Жёлтая рамка - WARNING violations
- Серый фон - Flagged stories

---

## Примечания

Эти правила применяются КО ВСЕМУ ПРОЕКТУ и должны соблюдаться во всех фичах, связанных с планированием, оценками, capacity и timeline.
