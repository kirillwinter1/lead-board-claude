# F25: Ручная сортировка эпиков и сторей (Manual Order)

## Статус: 🚧 В разработке (требуется рефакторинг)

## Цель

Позволить пользователю вручную задавать порядок эпиков и сторей на доске, чтобы отразить реальные приоритеты команды. Система рассчитывает AutoScore как рекомендацию, но финальное решение о порядке — за пользователем.

---

## Бизнес-требования

### БТ-1: Изменение порядка эпика

**Предусловия:**
- Выбрана одна команда (фильтр по команде)

**Сценарий:**
1. Пользователь захватывает эпик мышкой за drag-handle
2. Перетаскивает на нужную позицию в списке
3. Отпускает

**Ожидаемый результат:**
- Эпик остаётся на новом месте
- Порядок сохраняется в БД
- После перезагрузки страницы порядок сохранён
- Пересчитываются даты EXPECTED DONE для всех эпиков команды

### БТ-2: Изменение порядка стори внутри эпика

**Предусловия:**
- Выбрана одна команда
- Эпик раскрыт (видны стори)

**Сценарий:**
1. Пользователь захватывает стори мышкой за drag-handle
2. Перетаскивает на нужную позицию внутри того же эпика
3. Отпускает

**Ожидаемый результат:**
- Стори остаётся на новом месте
- Порядок сохраняется в БД
- После перезагрузки страницы порядок сохранён
- Пересчитываются даты EXPECTED DONE для сторей эпика

### БТ-3: Рекомендации системы

**Описание:**
Рядом с каждым эпиком/стори показывается индикатор рекомендации на основе AutoScore:

| Индикатор | Значение |
|-----------|----------|
| ✓ (зелёный) | Позиция совпадает с рекомендацией AutoScore |
| ↑ (синий) | Система рекомендует поднять выше |
| ↓ (серый) | Система рекомендует опустить ниже |

**Поведение:**
- Индикаторы носят рекомендательный характер
- Пользователь может игнорировать рекомендации
- При наведении показывается tooltip с рекомендуемой позицией

### БТ-4: Пересчёт EXPECTED DONE

**После изменения порядка:**
- Эпики выше в списке получают более ранние даты завершения
- Стори выше внутри эпика выполняются раньше
- Пересчёт происходит автоматически после сохранения нового порядка

### БТ-5: Ограничения

- Drag & drop доступен только при выборе одной команды
- Эпики перетаскиваются только внутри своей команды
- Стори перетаскиваются только внутри своего эпика
- Нельзя перетаскивать стори между эпиками

---

## Технические требования

### Хранение данных

**Поле `manual_order` в таблице `jira_issues`:**
- Тип: INTEGER
- Nullable: да (для обратной совместимости)
- Контекст сортировки:
  - Эпики: внутри `team_id`
  - Стори: внутри `parent_key`

### API

```
PUT /api/epics/{epicKey}/order
Body: { "position": number }  // 1-based
Response: { "issueKey": string, "manualOrder": number, "autoScore": number }

PUT /api/stories/{storyKey}/order
Body: { "position": number }  // 1-based
Response: { "issueKey": string, "manualOrder": number, "autoScore": number }
```

### Логика перемещения

При перемещении элемента на позицию N:
1. Все элементы с позицией >= N сдвигаются на +1
2. Элемент получает позицию N
3. Пересчитываются прогнозы (Unified Planning)

### Frontend

**Источник правды:** Backend (поле `manual_order`)

**Поток данных:**
1. Пользователь drag & drop
2. Оптимистичное обновление UI
3. API вызов для сохранения
4. Fetch актуальных данных с backend
5. Синхронизация UI с backend данными

---

## Известные проблемы (исправлены)

- ~~Рассинхронизация состояний~~ — исправлено миграцией на @dnd-kit
- ~~Сложная логика синхронизации~~ — упрощено: данные не меняются во время drag

## Улучшения на будущее (некритичные)

1. **Моргание при drop** — после API вызова `fetchBoard()` обновляет все данные, что вызывает кратковременное мерцание. Решение: оптимистичное обновление с плавным переходом.
2. **Стрелочки рекомендаций во время drag** — индикаторы могут меняться до завершения drop. Решение: заморозить рекомендации на время drag.

---

## Связанные фичи

- F19: Story AutoScore — расчёт рекомендаций
- F21: Unified Planning — пересчёт дат EXPECTED DONE
