# F13. Автопланирование — План реализации MVP

## Обзор этапов

| Этап | Описание | Зависимости | Статус |
|------|----------|-------------|--------|
| 1 | Производственный календарь | - | ✅ Готово |
| 2 | Конфигурация планирования команды | F5 | ✅ Готово |
| 3 | AutoScore — расчёт приоритета | F3 | ✅ Готово |
| 4 | Расчёт Expected Done | Этапы 1-3 | ✅ Готово |
| 5 | API прогнозирования | Этап 4 | ✅ Готово |
| 6 | UI — колонка Expected Done | Этап 5 | ✅ Готово |
| 7 | Ручной override приоритета | Этап 3 | ✅ Готово |
| 8 | UI Polish — улучшения интерфейса | Этапы 6-7 | ✅ Готово |

---

## Этап 1: Производственный календарь

### Задачи

1. **Интеграция с внешним API**
   - Подключить xmlcalendar.ru или isdayoff.ru
   - Получение списка выходных и праздников РФ
   - Кэширование на сутки

2. **Fallback для закрытых контуров**
   - Загрузка из конфигурационного файла (YAML/JSON)
   - Загрузка из таблицы БД

3. **Конфигурация источника**
   - Настройка в application.yml
   - Переключение между API / file / database

4. **API endpoint**
   - `GET /api/calendar/workdays?from=&to=`
   - Возвращает рабочие дни и праздники

5. **Сервис расчёта рабочих дней**
   - Метод `countWorkdays(from, to)`
   - Метод `addWorkdays(date, days)`
   - Метод `isWorkday(date)`

### Таблицы БД

```sql
CREATE TABLE calendar_holidays (
    id BIGSERIAL PRIMARY KEY,
    date DATE NOT NULL UNIQUE,
    name VARCHAR(255),
    country VARCHAR(2) DEFAULT 'RU',
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_calendar_holidays_date ON calendar_holidays(date);
CREATE INDEX idx_calendar_holidays_country ON calendar_holidays(country);
```

### Конфигурация

```yaml
planning:
  calendar:
    source: api  # api | file | database
    api-url: https://xmlcalendar.ru/data/ru/{year}/calendar.json
    file-path: /config/holidays.json
    cache-ttl: 86400  # секунды (24 часа)
    country: RU
```

### Тесты

- Получение праздников из API
- Fallback на файл при недоступности API
- Расчёт рабочих дней между датами
- Добавление N рабочих дней к дате

---

## Этап 2: Конфигурация планирования команды

### Задачи

1. **Расширение модели Team**
   - Добавить поля конфигурации планирования
   - Миграция БД

2. **API для конфигурации**
   - `GET /api/teams/{id}/planning-config`
   - `PUT /api/teams/{id}/planning-config`

3. **Валидация**
   - Коэффициенты грейдов > 0
   - Коэффициент рисков >= 0
   - WIP лимиты >= 1

### Таблицы БД

```sql
ALTER TABLE teams ADD COLUMN planning_config JSONB DEFAULT '{
    "gradeCoefficients": {
        "senior": 0.8,
        "middle": 1.0,
        "junior": 1.5
    },
    "riskBuffer": 0.2,
    "wipLimits": {
        "team": 6,
        "sa": 2,
        "dev": 3,
        "qa": 2
    }
}'::jsonb;
```

### DTO

```java
public record PlanningConfigDto(
    GradeCoefficients gradeCoefficients,
    BigDecimal riskBuffer,
    WipLimits wipLimits
) {
    public record GradeCoefficients(
        BigDecimal senior,  // default 0.8
        BigDecimal middle,  // default 1.0
        BigDecimal junior   // default 1.5
    ) {}

    public record WipLimits(
        Integer team,  // default 6
        Integer sa,    // default 2
        Integer dev,   // default 3
        Integer qa     // default 2
    ) {}
}
```

### Тесты

- CRUD конфигурации планирования
- Валидация значений
- Дефолтные значения для новых команд

---

## Этап 3: AutoScore — расчёт приоритета

### Задачи

1. **Сервис расчёта AutoScore**
   - Сбор факторов из эпика
   - Нормализация и взвешивание
   - Итоговый score (0-100)

2. **Факторы и веса**

| Фактор | Вес | Расчёт |
|--------|-----|--------|
| Статус | 20 | In Progress = 20, Backlog = 0 |
| Прогресс | 15 | progress * 15 |
| Due date | 25 | Экспоненциальный рост к дедлайну |
| Priority | 15 | Highest=15, High=10, Medium=5, Low=0 |
| Размер | 10 | Инверсия от estimate (меньше = больше) |
| Возраст | 10 | Логарифм от дней с создания |
| Ручной boost | 5 | Добавочный балл (0-5) |

3. **Хранение ручного приоритета**
   - Поле `manual_priority_boost` в таблице эпиков

### Таблицы БД

```sql
ALTER TABLE jira_issues
ADD COLUMN manual_priority_boost INTEGER DEFAULT 0;
```

### Интерфейс

```java
public interface AutoScoreCalculator {
    BigDecimal calculate(Epic epic);
    Map<String, BigDecimal> calculateFactors(Epic epic);
}
```

### Тесты

- Расчёт каждого фактора отдельно
- Комбинированный расчёт
- Сравнение эпиков по AutoScore
- Влияние ручного boost

---

## Этап 4: Расчёт Expected Done

### Задачи

1. **Сервис расчёта прогноза**
   - Получение остатка работы по эпикам
   - Распределение capacity команды
   - Учёт последовательности SA → DEV → QA
   - Расчёт дат с учётом календаря

2. **Алгоритм распределения**
   ```
   1. Отсортировать эпики по AutoScore (desc)
   2. Для каждого эпика:
      a. Рассчитать остаток по ролям (с учётом коэффициентов)
      b. Распределить доступный capacity
      c. Вычислить дату завершения каждой фазы
      d. Expected Done = дата завершения QA
   3. Учесть параллельность (несколько эпиков одновременно)
   ```

3. **Расчёт уверенности**
   - Проверка факторов
   - Итоговый уровень: high / medium / low

4. **Дельта от Due date**
   - Сравнение Expected Done с Due date
   - Положительная = опоздание, отрицательная = запас

### Модели

```java
public record EpicForecast(
    String epicKey,
    BigDecimal autoScore,
    Integer manualPriorityBoost,
    LocalDate expectedDone,
    Confidence confidence,
    Integer dueDateDeltaDays,
    RemainingByRole remainingByRole
) {
    public enum Confidence { HIGH, MEDIUM, LOW }

    public record RemainingByRole(
        RoleRemaining sa,
        RoleRemaining dev,
        RoleRemaining qa
    ) {}

    public record RoleRemaining(
        BigDecimal hours,
        BigDecimal days
    ) {}
}
```

### Тесты

- Расчёт для одного эпика
- Расчёт для нескольких эпиков с распределением capacity
- Учёт последовательности ролей
- Учёт выходных и праздников
- Расчёт уверенности

---

## Этап 5: API прогнозирования

### Задачи

1. **Endpoint получения прогноза**
   - `GET /api/planning/forecast`
   - Фильтры: teamId, статусы

2. **Endpoint пересчёта**
   - `POST /api/planning/recalculate`
   - Инвалидация кэша, запуск расчёта

3. **Endpoint обновления приоритета**
   - `PATCH /api/planning/epics/{epicKey}/priority`
   - Обновление manual_priority_boost

4. **Кэширование**
   - Кэш результатов расчёта
   - TTL или инвалидация при изменениях

5. **Scheduler**
   - Автоматический пересчёт (после Jira sync)

### API

```
GET /api/planning/forecast?teamId={id}&statuses=IN_PROGRESS,BACKLOG
Response: {
    "calculatedAt": "2026-01-23T12:00:00Z",
    "epics": [EpicForecast...]
}

POST /api/planning/recalculate
Response: { "status": "started", "estimatedTime": "5s" }

PATCH /api/planning/epics/{epicKey}/priority
Request: { "boost": 5 }
Response: { "epicKey": "LB-123", "newAutoScore": 87.5 }
```

### Тесты

- Получение прогноза через API
- Фильтрация по команде и статусам
- Пересчёт по запросу
- Обновление приоритета

---

## Этап 6: UI — колонка Expected Done

### Задачи

1. **Добавление колонки в Board**
   - Дата в формате "15 марта" или "15.03"
   - Индикатор уверенности (цветной круг)
   - Дельта от Due date (текст)

2. **Компонент ForecastCell**
   - Отображение данных
   - Tooltip с деталями

3. **Стили**
   - 🟢 Высокая уверенность + в срок или раньше
   - 🟡 Средняя уверенность или небольшое опоздание
   - 🔴 Низкая уверенность или значительное опоздание

4. **Загрузка данных**
   - Интеграция с `/api/planning/forecast`
   - Объединение с данными Board

### Компонент

```tsx
interface ForecastCellProps {
  expectedDone: string | null;
  confidence: 'high' | 'medium' | 'low';
  dueDateDelta: number | null;
  dueDate: string | null;
}

const ForecastCell: React.FC<ForecastCellProps> = ({...}) => {
  // Render
};
```

### Тесты

- Отображение даты
- Правильный цвет индикатора
- Tooltip с деталями
- Пустое состояние (нет прогноза)

---

## Этап 7: Ручной override приоритета

### Задачи

1. **UI для изменения приоритета**
   - Кнопка/иконка в строке эпика
   - Модальное окно или inline редактирование
   - Слайдер или кнопки +/-

2. **Интеграция с API**
   - Вызов `PATCH /api/planning/epics/{epicKey}/priority`
   - Обновление UI после изменения

3. **Визуализация boost**
   - Показать текущий boost
   - Индикатор что приоритет изменён вручную

### Тесты

- Изменение приоритета через UI
- Сохранение на бэкенде
- Обновление AutoScore после изменения

---

## Этап 8: UI Polish — улучшения интерфейса

### Задачи

1. **Колонка Expected Done всегда видна**
   - Загружать forecast для всех команд (не только выбранной)
   - Кэширование данных на клиенте
   - Оптимизация запросов

2. **Тултип с деталями расчёта**
   - При наведении на дату показывать:
     - Расписание фаз (SA: 23-25 янв, DEV: 26 янв - 10 фев, QA: 11-15 фев)
     - Остаток работы по ролям
     - Capacity команды
   - Объяснение почему такая дата

3. **Даты окончания для сторей**
   - Расширить бэкенд для расчёта дат подзадач
   - Показывать expected done в дочерних строках

4. **Знак "i" с подсказками**
   - Иконка информации рядом с заголовком колонки
   - Объяснение логики расчёта
   - Значение цветов индикаторов (HIGH/MEDIUM/LOW)

5. **Улучшение визуализации**
   - Анимация при обновлении данных
   - Скелетон при загрузке
   - Сортировка по Expected Done

### Компоненты

```tsx
// Тултип с деталями
interface ForecastTooltipProps {
  forecast: EpicForecast;
  teamCapacity: TeamCapacity;
}

// Иконка информации
interface InfoIconProps {
  title: string;
  content: React.ReactNode;
}
```

### Тесты

- Отображение тултипа при hover
- Корректные данные в тултипе
- Загрузка forecast для всех команд
- Отображение дат для сторей

---

## Чеклист готовности MVP

### Backend

- [x] Сервис производственного календаря
- [x] API календаря
- [x] Конфигурация планирования команды
- [x] Сервис AutoScore
- [x] Сервис расчёта прогноза
- [x] API прогнозирования
- [x] Scheduler пересчёта (после Jira sync)
- [x] Тесты AutoScoreCalculator (30+ тестов)
- [x] Тесты ForecastService (18 тестов)

### Frontend

- [x] Колонка Expected Done в Board
- [x] Компонент ExpectedDoneCell
- [x] UI изменения приоритета (drag & drop)
- [x] Интеграция с API прогнозирования
- [x] Тултип с деталями расчёта (фазы, остаток работы)
- [x] UnifiedProgressCell (прогресс + остаток)
- [x] Колонка видна всегда (для всех команд)
- [x] Иконка "i" с подсказками
- [ ] Даты для сторей/подзадач (требует изменения бэкенда)

### Инфраструктура

- [x] Миграции БД (V10, V11)
- [x] Конфигурация в application.yml
- [x] Документация API (API_PLANNING.md)

---

## Оценка трудозатрат (грубая)

| Этап | SA | DEV | QA |
|------|----|----|-----|
| 1. Календарь | 0.5 | 2 | 1 |
| 2. Конфиг команды | 0.5 | 1 | 0.5 |
| 3. AutoScore | 1 | 2 | 1 |
| 4. Расчёт прогноза | 1 | 3 | 1.5 |
| 5. API | 0.5 | 1.5 | 1 |
| 6. UI колонка | 0.5 | 2 | 1 |
| 7. Override приоритета | 0.5 | 1 | 0.5 |
| **Итого** | **4.5** | **12.5** | **6.5** |

*Человеко-дни, без учёта рисков*
